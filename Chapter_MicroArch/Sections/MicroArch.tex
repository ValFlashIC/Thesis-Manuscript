% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %% Microarchitecture %%
% Microarchitecture
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% add RAM, nop, RTL, CAM, pipeline
%cmo

\section{SpDCache Microarchitecture}
\label{sec:arch:microarch}

\lettrine{S}{pDCache} is implemented into an L1 data cache for a CVA6 RISC-V core, building upon an existing open-source high-performance set-associative data cache known as HPDcache~\cite{Fuguet2023_HPDcache}.
In contrast to traditional data caches, HPDcache introduces new features, including buffering techniques for memory writes, set-associative storage to manage read \gls{miss} requests, and dynamic configurability of each cache-line to operate in either write-back or write-through modes.
It is designed for high performance, utilizing a three-stage pipeline to handle multiple instructions while minimizing stall potential through out-of-order cache execution.

An overview of \acrlong{spdc}'s microarchitecture is shown in Figure~\ref{fig:arch:microarch}, where \textit{black} and \textit{blue} components are originally from HPDcache.
The first stage (\textit{st0}) decodes instructions and issues reads to the cache directory and data RAMs (\textit{Dir} and \textit{Data}).
These reads require one cycle to respond.
In the second stage (\textit{st1}), the response indicates whether a \gls{hit} or \gls{miss} occurred and if an \gls{eviction} is necessary.
The instruction proceeds to the final stage (\textit{st2}) only if a \gls{miss} or \gls{eviction} must be processed.
At \textit{st0}, the instruction is not yet consumed.
Depending on the response and the availability of cache resources at \textit{st1}, a \textit{nop} (\textit{no operation}) may be inserted in the pipeline, and the instruction may be placed in a \textit{replay table}.
The instruction is then replayed with the highest priority once its dependencies are cleared.
If the instruction does not need to be replayed at \textit{st1}, it is consumed and processed.
This pipeline serves as the foundation for implementing \acrlong{spdc}'s \gls{reduction} operations.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.56\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_MicroArchi.pdf}
    \caption{\acrlong{spdc} Microarchitecture, based on HPDcache}
    \label{fig:arch:microarch}
\end{figure}

The \textit{cache controller} coordinates several auxiliary units.
The \textit{miss handler}, allocated from \textit{st2}, tracks outstanding memory read requests and notifies the cache controller with a \textit{refill} request when responses arrive.
An optional \textit{write buffer} gathers recent write to memory, allowing merges of transactions that target the same cache-line.
The \textit{flush controller}, also allocated from \textit{st2}, reads cache-line data from the \textit{Data} RAMs and issues memory writes.
It temporarily stall the pipeline to prevent RAM conflicts.
HPDcache further provides an \textit{UC} unit to handle uncached transactions.
Finally, the \textit{CMO handler} performs internal cache operations (e.g., invalidations and flushes).
When active it takes control of the cache and can invoke the \textit{flush controller} if necessary.
Most of these units are reused and extended to implement \acrlong{spdc}.

HPDcache provides extensive static and dynamic configurability, making it a flexible foundation for further extensions.
To implement \acrlong{spdc} as well as a \gls{redC} baseline, we modified HPDcache to be highly configurable so that a single RTL implementation can produce multiple designs.
The RTL can be instantiated as a generic cache (the first baseline, \acrshort{gc}), as a \gls{redC} (the second baseline, \acrshort{redc}), or as \acrshort{spdc}, the solution presented in the previous chapters.

\begin{highlight}{Conclusion}{Red}
    TODO
\end{highlight}

\subsection{RTL Implementation of SpDCache}
\label{sec:arch:spdcache}

We configure the original HPDcache as a 4-way set-associative cache with 128 \glspl{set} and 64-byte cache-lines, resulting in a total cache size of 32~KiB.
We extend the three-stage pipeline to handle the new \acrshort{red} instruction.
Figure~\ref{fig:arch:microarch} provides an overview of \acrlong{spdc}'s microarchitecture, highlighting new blocks in \textit{red}, modified blocks in \textit{blue}, and untouched components in \textit{black}.
The overall architecture aligns with the design presented in Chapter~\ref{cha:spdc}, adapted here for hardware implementation.
Let us briefly describe the operation of the \acrshort{drc} under three scenarios: \glspl{hit}, \glspl{miss} with available cache-lines, and \glspl{miss} requiring \gls{eviction}.

In the case of a \gls{hit}, the value of the \acrshort{red} instruction is accumulated with its corresponding value in the cache.
At \textit{st1}, the cache value, read in \textit{st0} from \textit{Data}, is accumulated with the \acrshort{red} value using a dedicated accumulator (\textit{Acc}).
The result is written back to \textit{Data} at \textit{st2}.

In the case of a \gls{miss}, if there is an available cache-line, the value of the \acrshort{red} instruction is simply stored in this free entry of the cache.
A free cache-line is selected at \textit{st1}, and at \textit{st2}, the \acrshort{red} value is written to \textit{Data} along with zeros to reset the selected cache-line.
A write to \textit{Dir} is also performed to change the state of the cache-line from free to valid.

If a \gls{miss} occurs but no cache-line is free, an \gls{eviction} must be processed before handling the \acrshort{red} instruction.
The \acrshort{red} instruction is placed in the \textit{PrioRED table} to be replayed with higher priority than the \textit{replay table}.
Meanwhile, at \textit{st2}, the \textit{miss handler}, \textit{flush controller}, and \textit{accumulation table} are allocated to process the \gls{eviction}.
The \gls{eviction} is a ``fire-and-forget'' operation, meaning the \acrshort{red} instruction only needs to wait for the entry in \textit{Data} to be freed, not for the entire \gls{eviction} to finish.
The \gls{eviction} process begins by fetching and resetting the chosen 64~B cache-line from \textit{Data}, which is done in one cycle by the \textit{flush controller}.
Simultaneously, the \textit{miss handler} issues a read to the main memory, which in our performance simulations, we assume will take 64 cycles to respond.
The \textit{accumulation table} waits and temporarily stores both the old cache-line from the \textit{flush controller} and the 64~B response data from the \textit{miss handler}.
It then accumulates the cache-line with the data, word by word, and sends the 64~B results back to the main memory using the existing write interface.

With the changes described above, we have transformed \mbox{HPDcache} into a \gls{redC}.
To achieve multiple cache regions, we virtually separate the cache RAMs.
To maintain HPDcache's set-associative operation, we divide the cache memory along its \glspl{set}.
Thus, each region of the cache has the same structure as the overall cache but is smaller in size: each \gls{set} contains a fixed number (the number of ways) of 64-byte cache-lines, as depicted in Figure~\ref{fig:arch:set_redirection}.
Each region can map any possible address.
To ensure that every address matches a unique \gls{set} within each region, we allocate a power-of-two number of consecutive \glspl{set} to a region.
Considering the total number of \glspl{set} $S_T$ and our region $R$ mapping on $S_R < S_T$ of them, starting at the \gls{set} $s_\mathit{offset}$, if an address matches one of the \glspl{set} of this region, no action is required.
However, if the address matches a \gls{set} $s_\mathit{addr}$ outside the region, we need to map it to a unique \gls{set} in the region such that $s_\mathit{region} = (s_\mathit{addr} \% S_R) + s_\mathit{offset}$, as illustrated in Figure~\ref{fig:arch:set_redirection}.
The three regions \acrshort{grc}, \acrshort{drc}, and \acrshort{srt} are each defined by their number of \glspl{set} $S_R$ and their first \gls{set}, $s_\mathit{offset}$.
\acrlong{spdc} can thus be reconfigured to operate as a generic cache, a traditional \gls{redC} or a region-based \gls{redC} based on the needs of the application (\textit{config} in Figure~\ref{fig:arch:microarch}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_SetsRedirection.pdf}
    \caption{Set Redirection Depending on the Region}
    \label{fig:arch:set_redirection}
\end{figure}

The \acrshort{srt} region requires additional adjustments to function as an element-wise fully-associative cache.
This region operates as if all the \glspl{set} were combined into a single \gls{set}, containing numerous elements.
Each element requires its own directory entry.
Therefore, we replace the \textit{Dir} RAMs of this region with a \textit{CAM} (\textit{Content-Addressable Memory}) containing the addresses of each valid element in the \acrshort{srt} region.
With the \textit{CAM}, we can check for a \gls{hit} across the entire region and retrieve the matching value stored in the \textit{Data} RAMs.
We also added a queue (\textit{RMWQ}) to manage the element-wise \gls{rmw} memory transactions.

To avoid address duplication between regions, the \textit{MultiHit fetcher} in Figure~\ref{fig:arch:microarch} manages coherency between the \acrshort{drc} and \acrshort{srt}, following the description in Chapter~\ref{cha:spdc}.
Since software selects the region, this can be necessary.
The \textit{MultiHit fetcher} performs two tasks.
First, given an address, it probes the \textit{CAM} for entries whose cache-line address matches.
If one or more matches are found, it records their positions locally and notifies the \textit{cache controller}.
Second, when activated, the \textit{MultiHit fetcher} gathers the corresponding elements from the \textit{Data} RAMs of the \acrshort{srt} and assembles them into a single 64~B line that will be written into the \acrshort{drc}.

Contrary to the description in Chapter~\ref{cha:spdc}, we do not transfer isolated elements from the \acrshort{drc} to the \acrshort{rmwq} on \gls{eviction}.
In our Python simulations we observed that only a negligible fraction of elements were transferred (mean 0.3\%, range 0 to 5\% across the 48 matrices), which demonstrates the effectiveness of the \acrshort{drc} blocking behavior.
To simplify the RTL implementation, this transfer is therefore omitted.

\begin{highlight}{Conclusion}{Red}
    TODO
\end{highlight}

\subsection{Software Interface}
\label{sec:arch:sw_interface}

We have added several custom instructions to the CVA6 RISC-V core~\cite{Waterman2016_RISCV_ISA}.
The primary addition is the \gls{reduction} instruction (\acrshort{red}), which takes an address and a value as arguments and expects no response.
This instruction is accompanied by a region instruction (\acrshort{reg}) that specifies in which region subsequent \glspl{reduction} should be processed.
We have also added an instruction to flush the \gls{reduction} regions (\acrshort{drc} and \acrshort{srt}) of the cache, which is necessary at the end of the algorithm.
It is performed in the cache by the \textit{CMO handler}.
Finally, we have included an instruction to enable and disable the \gls{reduction} mode of \acrlong{spdc}.

We demonstrate a simple example in Listing~\hyperlink{hl0}{1} that accumulates multiple values at a single address.
Before executing \acrshort{red} transactions, the \gls{reduction} engine must be enabled (line~3).
The cache transitions from a fully generic state to \acrshort{spdc} with multiple regions using a hardware-defined static configuration.
This example assumes a configuration with three regions: \acrshort{grc}, \acrshort{drc}, and \acrshort{srt}.
For a simpler \gls{redC} configuration with only two regions (\acrshort{grc} and \acrshort{drc}), the \acrshort{reg} instruction would be unnecessary.
Additionally, the instruction can accept a dynamic configuration argument specifying word granularity and operation type.
A future optimization would allow passing the entire configuration as a parameter, enabling dynamic configuration of \acrshort{spdc} regions at runtime.
Next, at line~8, we select the \acrshort{ibr} region using the \acrshort{reg} instruction.
The subsequent two \acrshort{red} instructions are then executed and stored in the \acrshort{drc}.
At line~13, we switch to the \acrshort{obr} region, causing the following \acrshort{red} instruction to be processed by the \acrshort{srt}.
Once all \acrshort{red} operations complete, \acrlong{spdc} is flushed at line~17 before disabling the reduction engine, reverting to standard cache operation.

\begin{figure}[t]
    \hypertarget{hl0}{}
    \begin{minipage}{\linewidth}
        \addtolength{\linewidth}{-16pt}
        \colorbox{white}{\begin{minipage}[t][5mm][t]{6.5pt}\null\hfill\null\end{minipage}}
        \begin{minipage}{\linewidth}
            \lstinputlisting[
                title=\textbf{Listing 1:} A Simple Example of \acrshort{red} Operations Usage
            ]{Chapter_MicroArch/Code/red_example.c}
        \end{minipage}
        %\bigskip
    \end{minipage}
\end{figure}

\begin{highlight}{Conclusion}{Red}
    TODO
\end{highlight}

\subsection{Details on SpDCache's Pipeline Operation}
\label{sec:arch:pipeline}

In this section, we dive into the details of the \acrlong{spdc}'s pipeline architecture.
It is based on the HPDcache original pipeline, with the aim of only adding additional behavior for our needs without destroying original operating.
On Figures~\ref{fig:arch:pipeline_IBR} and~\ref{fig:arch:pipeline_OBR}, we illustrate the various scenarios that can occur when processing \acrshort{red} instructions, depending on the selected region (\acrshort{ibr} or \acrshort{obr}, respectively).
We represents the three pipeline stages (\textit{st0}, \textit{st1}, and \textit{st2}) and the interactions with the auxiliary units involved in processing \acrshort{red} instructions.
The \textit{green}, \textit{red} and \textit{blue} arrows represents reads, writes and allocations of the units, respectively.
The \textit{Dir} and \textit{Data} RAM units, notified with a hourglass, always need one full cycle to respond.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_01.pdf}
        \caption{\Gls{hit} in the \acrshort{drc}}
        \label{fig:arch:pipeline_01}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_02.pdf}
        \caption{\Gls{miss} in the \acrshort{drc}}
        \label{fig:arch:pipeline_02}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_03.pdf}
        \caption{\Gls{miss}+\Gls{eviction} in the \acrshort{drc}}
        \label{fig:arch:pipeline_03}
    \end{subfigure}
    %
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=0.8\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_06.pdf}
        \caption{Cache-line \Gls{eviction} Process}
        \label{fig:arch:pipeline_06}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_04.pdf}
        \caption{\Gls{miss} in the \acrshort{drc} but ``Multi-\Gls{hit}'' in the \acrshort{srt}}
        \label{fig:arch:pipeline_04}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_05.pdf}
        \caption{\Gls{miss} in the \acrshort{drc} but \Gls{hit} in the \textit{MultiHit Fetcher}}
        \label{fig:arch:pipeline_05}
    \end{subfigure}
    \caption{Pipeline Operation when the Region Argument is \acrshort{ibr}}
    \label{fig:arch:pipeline_IBR}
\end{figure}

We start with the cases when the region argument is \acrshort{ibr}.
In Figure~\ref{fig:arch:pipeline_01}, reads are issued to the \textit{Dir} and \textit{Data} RAMs at \textit{st0}.
The answers are received one cycle later, at \textit{st1}, which identify if there is a \gls{hit} or a \gls{miss} in the \acrshort{drc}.
This first part of the pipeline is the same for every cases.
In the case \ding{192}, a \gls{hit} occurred, thus the data, also received at \textit{st1}, is accumulated with the \acrshort{red} value and sent to \textit{st2} to be write back in the \textit{Data} RAM.

If a \gls{miss} occurs, several cases are possible, from Figures~\ref{fig:arch:pipeline_02} to~\ref{fig:arch:pipeline_05}, since an \gls{eviction} can occur and the \textit{MultiHit Fetcher} is bing used.
In the case \ding{193}, when receiving a \gls{miss} at \textit{st1}, a check for a ``multi-\gls{hit}'' is performed in the \textit{Dir} CAM of the \acrshort{srt}.
Indeed, as presented in Chapter~\ref{cha:spdc}, in case of \gls{miss} in the \acrshort{drc}, we want to gather elements of the same cache-line address that could be in the \acrshort{srt} to avoid duplications.
The ``multi-\gls{hit}'' operation is done within the cycle, thus responding to the \textit{st1}, and also notifying the \textit{MultiHit Fetcher}.
In case of ``multi-\gls{hit}'', the \textit{MultiHit Fetcher} will allocate an entry for the cache-line address into local memory and will take the control of the pipeline to read and invalidate the ``multi-\gls{hit}'' entries from the \textit{Data} RAM and write it locally to be used later.
In the case \ding{193}, there is no ``multi-\gls{hit}'' and the \textit{MultiHit Fetcher} is empty.
We are thus dealing with the simple \gls{miss} case, which necessitate writing the \acrshort{red} value into a reset cache-line in the \textit{Data} RAM at \textit{st2}.

In the case \ding{195} however, the ``multi-\gls{hit}'' occurs, showing the allocation of the \textit{MultiHit Fetcher}.
In this case, the \acrshort{red} instruction must be placed in the \textit{PrioRED Table} to be replayed immediately as soon as the pipeline is liberated, towards cases \ding{194} or \ding{196} depending on the need of \gls{eviction} or not, respectively.

In the case \ding{196}, which only occurs upon replay, the \acrshort{red} instruction \glspl{miss} again but at \textit{st1}, it \glspl{hit} on the \textit{MultiHit Fetcher} which has gathered the data on the previous run.
Thus, the \acrshort{red} value is accumulated with the \textit{MultiHit Fetcher} data to be write in the \textit{Data} RAM at \textit{st2}.

The last case, \ding{194}, occurs when an \gls{eviction} is needed, being similar with or without replays from the ``multi-\gls{hit}''.
If an \gls{eviction} is needed, a victim cache-line is selected at \textit{st1} and must be removed from the \textit{Data} RAM before inserting the new value.
Thus, the \gls{eviction} process is launched at \textit{st2}, the \textit{Miss Handler}, the \textit{Flush Controller} and the \textit{Accumulation Table} being allocated with the victim address.
Meanwhile, the \acrshort{red} instruction is put in the \textit{PrioRED Table} to be replayed immediately.
As soon as the victim cache-line is freed, the \acrshort{red} instruction can be replayed following the case \ding{193} or \ding{196}, depending on the presence of data on the same cache-line address in the \textit{MultiHit Fetcher} or not, respectively.
Note that as soon as there is a ``multi-\gls{hit}'' (case \ding{195}), the \textit{MultiHit Fetcher} processes the data gathering from the \acrshort{srt}, and keeps the data up until we get to the case \ding{196}.
After that, since the data is no more in the\textit{Dir} CAM and \textit{Data} RAM, there will be no ``multi-\gls{hit}'' on the same address upon replay.

In Figure~\ref{fig:arch:pipeline_06}, we present the \gls{eviction} process, that works in parallel of the pipeline.
When receiving the victim address from the pipeline in case \ding{194}, the \textit{Flush Controller} immediately fires a read in the \textit{Data} RAM followed by an invalidation.
It is done in one cycle, allowing the pipeline to replay the \acrshort{red} instruction without having to wait for the entire \gls{eviction} process to finish.
The \textit{Flush Controller} then sends its victim data to the \textit{Accumulation Table} that is temporarily storing it.
Meanwhile, the \textit{Miss Handler} issues a memory read, which can take up to a hundred cycles in the worst cases.
Upon reception of the data from memory, the \textit{Miss Handler} also sends the dada to the \textit{Accumulation Table} to be stored.
As soon as the \textit{Accumulation Table} has the two data lines, it accumulates them, word by word, and sends the results back to the \textit{Flush Controller} that serves as a memory write interface to write the result back in memory and complete the \gls{eviction}.

Here is the list of the possible sequences of cases that can occur when the region argument is \acrshort{ibr}:
\begin{itemize}
    \item \gls{hit} in the \acrshort{drc}, \ding{192};
    \item \gls{miss} in the \acrshort{drc}, \ding{193};
    \item \gls{miss}+\gls{eviction} in the \acrshort{drc}, \ding{194} + \ding{197} $\rightarrow$ \ding{193};
    \item \gls{miss} in the \acrshort{drc} with ``multi-\gls{hit}'' in the \acrshort{srt}, \ding{195} $\rightarrow$ \ding{196};
    \item \gls{miss}+\gls{eviction} in the \acrshort{drc} with ``multi-\gls{hit}'' in the \acrshort{srt}, \ding{195} $\rightarrow$ \ding{194} + \ding{197} $\rightarrow$ \ding{196}.
\end{itemize}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_07.pdf}
        \caption{\Gls{hit} in the \acrshort{drc}}
        \label{fig:arch:pipeline_07}
    \end{subfigure}

    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_08.pdf}
        \caption{\Gls{hit} in the \acrshort{srt}}
        \label{fig:arch:pipeline_08}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_09.pdf}
        \caption{\Gls{miss} in the \acrshort{srt}}
        \label{fig:arch:pipeline_09}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Chapter_MicroArch/Figures/PDF/SpDC_Pipeline_10.pdf}
        \caption{\Gls{miss}+\Gls{eviction} in the \acrshort{srt}}
        \label{fig:arch:pipeline_10}
    \end{subfigure}
    \caption{Pipeline Operation when the Region Argument is \acrshort{obr}}
    \label{fig:arch:pipeline_OBR}
\end{figure}

We now describe the cases when the region argument is \acrshort{obr}, from Figures~\ref{fig:arch:pipeline_07} to~\ref{fig:arch:pipeline_10}.
In every cases, we check for \glspl{hit} both in the \acrshort{drc} and \acrshort{srt} at \textit{st0}.
If there is a \gls{hit} in the \acrshort{drc}, case \ding{198}, we enter the special case where, regardless of the region argument, the \acrshort{red} instruction is processed in the \acrshort{drc}.
It is thus sent to the \textit{PrioRED Table} to be replayed as a \acrshort{drc} \gls{hit}, on case \ding{192}.

If there is no \textit{hit} in the \acrshort{drc}, we simply check the \textit{Dir} CAM for \glspl{hit} in the \acrshort{srt}.
In case of \gls{hit}, \ding{199}, the data is accumulated with the \acrshort{red} value and write back in the \textit{Data} RAM.
In case of \gls{miss}, \ding{200}, the data is inserted in the the \textit{Data} RAM.
In the case of an \gls{eviction}, \ding{201}, the victim data can be read directly between \textit{st0} and \textit{st1}, since the \textit{Dir} CAM do not need a full cycle to respond and thus can initiate the victim read before \textit{st1}.
Thus, the \acrshort{red} value can be inserted in the \textit{Data} RAM at \textit{st2}, while the victim data is sent to the \acrshort{rmwq}.
The \acrshort{rmwq} independently fires atomic \acrshort{rmw} transaction to memory when not empty.

Here is the list of the possible sequences of cases that can occur when the region argument is \acrshort{obr}:
\begin{itemize}
    \item \gls{hit} in the \acrshort{drc}, \ding{198} $\rightarrow$ \ding{192};
    \item \gls{hit} in the \acrshort{srt}, \ding{199};
    \item \gls{miss} in the \acrshort{srt}, \ding{200};
    \item \gls{miss}+\gls{eviction} in the \acrshort{srt}, \ding{201}.
\end{itemize}

\begin{highlight}{Conclusion}{Red}
    TODO
\end{highlight}