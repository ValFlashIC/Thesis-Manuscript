%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CHAPTER Background
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Background}
\label{cha:background}

\minitoc

% \input{}

%%%%%%%%%%%%%%%%%%%%

\note{From SoA paper}

\section{Sparse Matrix Formats and Algorithms}
\label{sec:background:matrix_formats_and_algos}

% %%%%%%%%%%%%%%%%%%%%%%%%% Sparse Formats %%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Sparse Formats}
\label{sec:background:matrix_formats}

In the absence of a compressed format, matrices are stored in fixed size two-dimensional arrays. With this \emph{dense} format, elements can be randomly accessed ($\mathcal{O}(1)$), and sequential accesses are highly efficient. The two dimensions are called \emph{ranks} which correspond to the rows and columns. For matrices, there are thus two variants of the dense format, depending on which \emph{rank} is stored sequentially in memory. In a \emph{row-wise} (or \emph{row-major}) storage, the elements within the rows are sequential, and conversely for \emph{column-wise} (or \emph{column-major}). 

Sparse matrices contain a very large number of zeros, and to avoid storing these repeated zeros, there exist many compressed formats~\cite{Tewarson1973_COO_CSR_LIL_first, Kincaid1989_ELLPACK, Barrett1994_CSR_BCSR_DIA_ELL, Saad2003_COO_CSR_DIA_ELL, Bell2009_HYB, Chou2018_COO_CSR_DIA_ELL_HASH}, where certain formats are best-suited for matrices with a specific structure. The main principle of these \emph{sparse formats} is to avoid storing the zero elements, compensating with additional \emph{metadata} which gives the position of the non-zero elements.
%%%% VC %%%%
We can cite some of the most used sparse formats like \emph{COO} (\emph{COOrdinate}) that uses three tables of size $NNZ$ with only the non-zero elements (\emph{Payload} table in Figure~\ref{fig:background:FormatEx:COO}) and their row and column indices (\emph{Row} and \emph{Col IDX} tables), or \emph{CSR} (\emph{Compressed Sparse Row}) (and its \emph{column-major} counterpart \emph{CSC}) that stores pointers to row (or column) positions in the third table instead of indices (\emph{Row PTR} table in Figure~\ref{fig:background:FormatEx:CSR}).
%%%% VL %%%%
% \begin{itemize}
%     \item The simplest \emph{sparse format} is called \emph{COO} (\emph{COOrdinate})~\cite{Tewarson1973_COO_CSR_LIL_first, Saad2003_COO_CSR_DIA_ELL, Chou2018_COO_CSR_DIA_ELL_HASH}. It consists in three separate tables, one filled only with the non-zero elements of the matrix, a row index table containing the row of the corresponding elements, and similarly a column index table (see Figure~\ref{fig:FormatEx:COO}). The two latter are \emph{metadata} tables and all three tables are of size \emph{NNZ}. In this format the elements are not necessarily sorted, the memory footprint does not depend on the structure of the matrix and all elements are stored independently of each other.
%     %
%     \item The most commonly used \emph{sparse format} is called \emph{CSR} (\emph{Compressed Sparse Row})~\cite{Tewarson1973_COO_CSR_LIL_first, Barrett1994_CSR_BCSR_DIA_ELL, Saad2003_COO_CSR_DIA_ELL, Chou2018_COO_CSR_DIA_ELL_HASH}. It is similar to the \emph{COO} format except that the row index table is replaced by a set of indices that point to the position of the start of each row in the column index table (see Figure~\ref{fig:FormatEx:CSR}). The size of this table is the number of rows plus one, and, in practical cases, the memory footprint is smaller than with \emph{COO}. However, the non-zero elements need to be sorted in \emph{row-wise} order. This format has a \emph{column-wise} variant called \emph{CSC} (\emph{Compressed Sparse Column}) where the column index table stores the positions of columns instead of rows. These two formats are symmetrical but with \emph{CSR}, it is the traversal of rows that is efficient because they are sequential whereas in \emph{CSC}, column traversal is efficient.
%     %
%     \item Even in sparse matrices, there can be regions, or blocks, that have a large fraction of non-zero values. In this case, the \emph{BCSR} format~\cite{Barrett1994_CSR_BCSR_DIA_ELL}, which is an extension of the \emph{CSR} format with \emph{tiling}, can be attractive. We use the term \emph{tiling} to describe a level of hierarchy in a storage format. That is, a \emph{tile} refers to a sub-matrix, which itself may be dense, as in the case of \emph{BCSR}, or which could be sparse. With the \emph{BCSR} format, we simply replace the non-zero elements of the \emph{CSR} format with dense, fixed-size sub-matrices (see Figure~\ref{fig:FormatEx:BCSR}). Within these sub-matrices, it is possible to have some zero elements. The advantage of the \emph{BCSR} format is that the dense sub-matrices can be processed using techniques optimized for dense matrices, particularly using vector units or GPUs~\cite{Im2004_TilingBCSR}.
%     Compared to the \emph{CSR} format, \emph{BCSR} makes it possible to accelerate the computation within the dense \emph{tiles}, but this comes with the overhead of storing some zero values. Not all matrices are well suited to the \emph{BCSR} format.
%     %
%     \item For diagonally structured matrices
%     ($\forall(i, j) \in \left[0, M\right[ \times \left[0, N\right[, \; a_{i,j}=0 \; for \; \lvert i-j \rvert > K$), a format called \emph{DIA} (\emph{DIAgonal}) is particularly well suited~\cite{Barrett1994_CSR_BCSR_DIA_ELL, Saad2003_COO_CSR_DIA_ELL, Chou2018_COO_CSR_DIA_ELL_HASH}. It consists in storing the values along the $2 \times K+1$ diagonals in a table and keeping information about the position of the diagonals in the matrix (see Figure~\ref{fig:FormatEx:DIA1}). The organization of the non-zero data and selection of the diagonals can be tailored to the matrices and access patterns. This format is well suited to diagonal matrices, has a low overhead for storing indices and can be combined with other formats to better fit more complex structures.
%     %
%     \item Another format called \emph{ELL} (\emph{ELLPACK})~\cite{Kincaid1989_ELLPACK, Barrett1994_CSR_BCSR_DIA_ELL, Saad2003_COO_CSR_DIA_ELL, Chou2018_COO_CSR_DIA_ELL_HASH} is well suited when the number of non-zero elements per row can be bounded by $K$. In this case, the non-zero values in a $M \times N$ matrix can be stored as a dense $M \times K$ matrix, augmented with an index table, of the same dimensions, which gives the column position of each non-zero value (see Figure~\ref{fig:FormatEx:ELL}). This format is not efficient if the number of non-zero elements per row is highly variable, as many entries in the $M \times K$ table are unused. This format is well suited to GPUs~\cite{Vazquez2009_ELLforGPUs, Cao2010_ELLforGPUs} since it transforms a sparse matrix into a dense one.
%     %
%     \item The \emph{HYB} format (HYBrid)~\cite{Bell2009_HYB} is an example of a format which combines \emph{ELL} and
%     \emph{COO}. The \emph{ELL} format can be used, but with a value of $K$ (the width
%     of the value table) that is smaller than the maximum non-zero entries per row in the 
%     original matrix (see Figure~\ref{fig:FormatEx:HYB}). For the small number of rows where the table lacks a sufficient number of columns,
%     the \emph{COO} format can be used to store the extra values. In this way, the storage efficiency
%     of \emph{ELL} is improved, and it can be applied to a broader class of matrices.
% \end{itemize}
%%%%%%%%%%%%

\begin{figure}
    \centering
    \begin{minipage}[b]{0.66\linewidth}
        \begin{figure}[H]
            \centering
            \begin{subfigure}[t]{0.19047619\textwidth} % 4/16 % /21
                \centering
                \includegraphics[width=\textwidth]{Chapter_Background/Figures/PDF/SparseFormats/Dense_ShortVersion}
                \caption{\textbf{Dense}: \emph{U$^{2}$}}
                \label{fig:background:FormatEx:Dense}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.357142857\textwidth} % 7.5/16 
                \centering
                \includegraphics[width=\textwidth]{Chapter_Background/Figures/PDF/SparseFormats/COO_ShortVersion.pdf}
                \caption{\textbf{COO}: \emph{CP$^{2}$ (row-maj. sorted)}}
                \label{fig:background:FormatEx:COO}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.357142857\textwidth} % 7.5/16
                \centering
                \includegraphics[width=\textwidth]{Chapter_Background/Figures/PDF/SparseFormats/CSR_ShortVersion.pdf}
                \caption{\textbf{CSR}: \emph{(UOP, CP) row-maj.}}
                \label{fig:background:FormatEx:CSR}
            \end{subfigure}
            \caption{Example of a Sparse Matrix in \textit{COO} and \textit{CSR} Formats}
            \label{fig:background:FormatEx}
        \end{figure}
    \end{minipage}
    \hfill
    \begin{minipage}[b]{0.30\linewidth}
        \begin{figure}[H]
            \centering
            \includegraphics[width=\textwidth]{Chapter_Background/Figures/PDF/FiberTreeEx_JustCSR.pdf}
            \caption{Example of the \emph{FiberTree} Representation with \emph{CSR} Format}
            \label{fig:background:FiberTreeExCSR}
        \end{figure}
    \end{minipage}
\end{figure}

A visual representation of compressed matrices, called \emph{FiberTree}~\cite{Sze2020_FiberTree}, is helpful to understand these \emph{sparse formats}. Each \emph{rank} of a matrix represents a level of a tree where the nodes are filled with either the \emph{metadata} of the child nodes or the matrix data which can only appear in the leaves (see Figure~\ref{fig:background:FiberTreeExCSR}). With this representation, we clearly identify the number of \emph{ranks}, which define the \emph{fibers}. A \emph{fiber} is the generic term to describe a one-dimensional stream of data or \emph{metadata}, which corresponds to a row or column in the case of a two-dimensional matrix. A stream of indices is a \emph{fiber} of \emph{metadata}.
Figure~\ref{fig:background:FiberTreeExCSR} presents, as an example, a \emph{FiberTree} representation of the matrix of Figure~\ref{fig:background:FormatEx:Dense}, matching the \emph{CSR} format: each \emph{rank} in the tree corresponds to a \emph{fiber} stored in the table \emph{Row PTR} or \emph{Col IDX} of Figure~\ref{fig:background:FormatEx:CSR}.

% \begin{figure}
%     \centering
%     \begin{subfigure}{0,386995238\textwidth} % 330.73 / 854.61
%         \centering
%         \includegraphics[width=\textwidth]{Figures/PDF/FiberTreeEx_CPCP.pdf}
%         \caption{\emph{(CP, CP) row-major}}
%         \label{fig:FiberTreeExCPCP}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{0,300312423\textwidth} % 256.65 / 854.61
%         \centering
%         \includegraphics[width=\textwidth]{Figures/PDF/FiberTreeEx_COO.pdf}
%         \caption{\textbf{COO}: \emph{$CP^2$ row-major}}
%         \label{fig:FiberTreeExCOO}
%     \end{subfigure}
%     \hfill
%     \begin{subfigure}{0,300312423\textwidth} % 256.65 / 854.61
%         \centering
%         \includegraphics[width=\textwidth]{Figures/PDF/FiberTreeEx_CSR.pdf}
%         \caption{\textbf{CSR}: \emph{(UOP, CP) row-major}}
%         \label{fig:FiberTreeExCSR}
%     \end{subfigure}
%     \caption{Some Examples of the FiberTree Representation}
%     \label{fig:FiberTreeEx}
% \end{figure}

In a recent work~\cite{Wu2022_SparseLoop}, a systematic nomenclature for \emph{sparse matrix (and tensor) formats} was proposed, based on \emph{FiberTree} representation. In fact, in the example of Figure~\ref{fig:background:FiberTreeExCSR}, the \emph{fibers} are compressed with different methods. Thus each \emph{rank} can be characterized by a label indicating how it is compressed: \emph{Uncompressed} (\emph{U}), \emph{Coordinate Payload} (\emph{CP}), \emph{Uncompressed Offset Pairs} (\emph{UOP}).
\emph{U} defines \emph{ranks} where all data values are stored contiguously in memory. \emph{CP} defines \emph{ranks} that store the indices of each payload, a payload being either a non-zero element or a sub-matrix\footnote{In this section, we use the term sub-matrix but this is extended to the notion of \emph{tiling} in Section~\ref{sec:background:tiling}.}. \emph{UOP} defines \emph{ranks} that store pointers (offsets) to delimit sub-matrices of the next \emph{rank}\footnote{If some sub-matrices are empty, pointers will be repeated, hence 'uncompressed'.}.
These are the three main ones as they are used in the most common \emph{sparse formats} but many others exist or can be created.

%%%% VL %%%%
% These are described below and summarized in Table~\ref{tab:SAFs}.
% \begin{itemize}
%     \item \emph{U} (\emph{Uncompressed}) defines \emph{ranks} where all data values are stored contiguously in memory.
%     \item \emph{CP} (\emph{Coordinate Payload}) defines \emph{ranks} that store the indices of each payload. A payload can be either a non-zero element or a \emph{tile}.
%     \item \emph{UOP} (\emph{Uncompressed Offset Pairs}) defines \emph{ranks} that store pointers (offsets) to delimit \emph{tiles} of their next \emph{rank}. If some \emph{tiles} are empty, pointers will be repeated, hence 'uncompressed'.
%     \item \emph{B} (\emph{Bitmask}) defines \emph{ranks} that store a bitmap for the payloads (data or \emph{tiles}) with the value one marking non-zero payloads.
%     \item \emph{RLE} (\emph{Run Length Encoding}) defines \emph{ranks} that store the numbers of zero (data or \emph{tiles}) present before each non-zero element.
% \end{itemize}
% This list is not exhaustive but covers the most common \emph{SAF}s.
%%%%%%%%%%%%

%%%% VC %%%%
With this nomenclature, the \emph{sparse formats} can be classified according to their \emph{rank} compression methods. For example, the \emph{CSR} format is denoted as \emph{(UOP, CP) row-major} since its \emph{Row PTR} table stores pointers (offsets) to rows. The \emph{CSR} and \emph{CSC} formats are both of type \emph{(UOP, CP)}, one being \emph{row-major}, the other \emph{column-major}. The \emph{COO} format is denoted as $CP^2$ because there are two coordinates (row and column) for each non-zero element, as it is a \emph{CP fiber} having two indices per data element instead of one.
%%%% VL %%%%
% With this nomenclature, that can be easily extended, the \emph{sparse formats} described earlier can be classified according to the \emph{SAFs}, as shown in Table~\ref{tab:SAFs_ex}. For example, the \emph{COO} format is described as $CP^2$ because there are two coordinates (row and column) for each payload element, which, in the case of \emph{COO}, are simply the non-zero elements.
%%%%%%%%%%%%
User defined formats can also be easily described and named. For example, a user could define a storage format with a nested \emph{CSR}. That is, each non-zero element in the outer \emph{CSR}, corresponds to a sub-matrix, itself stored in \emph{CSR} format. Such a representation would be labeled \emph{(UOP, CP, UOP, CP)}.
A similar nomenclature is proposed by TACO~\cite{Chou2018_COO_CSR_DIA_ELL_HASH}. It is more adapted to code generation applications, where the \emph{FiberTree}-based nomenclature better describes the memory storage of the \emph{sparse format}. For example, \emph{U} corresponds to \emph{Dense}, \emph{CP} to \emph{Singleton}, the pair \emph{(UOP, CP)} is translated as \emph{Compressed} while \emph{UOP} alone corresponds to \emph{Offset}. We chose to adopt the \emph{FiberTree}-based nomenclature in this article since it is more suited to a hardware context.

% \begin{figure}
%     \centering
%     \begin{minipage}[t]{0.39\linewidth}
%         \begin{table}[H] %htbp
%             \begin{center}
%                 \caption{\emph{FiberTree}-based vs TACO Nomenclatures}
%                 \begin{tabular}{|c|c|}
%                     \hline
%                     \textbf{\emph{FiberTree}-based}~\cite{Wu2022_SparseLoop} & \textbf{TACO}~\cite{Chou2018_COO_CSR_DIA_ELL_HASH} \\
%                     \hline
%                     \emph{U} & Dense \\
%                     \hline
%                     \emph{CP} & Singleton \\
%                     \hline
%                     \emph{(UOP, CP)} & Compressed \\
%                     \hline
%                     \emph{UOP} & Offset \\
%                     \hline
%                 \end{tabular}
%                 \label{tab:FTvsTACO}
%             \end{center}
%         \end{table}
%     \end{minipage}
%     \hfill
%     \begin{minipage}[t]{0.59\linewidth}
%         \begin{table}[H] %htbp
%             \begin{center}
%                 \caption{\emph{FiberTree}-based vs TACO \emph{Sparse Format} Examples}
%                 \begin{tabular}{|c|c|}
%                     \hline
%                     \textbf{\emph{FiberTree}-based}~\cite{Wu2022_SparseLoop} & \textbf{TACO}~\cite{Chou2018_COO_CSR_DIA_ELL_HASH} \\
%                     \hline
%                     $U^2$ & (Dense, Dense) \\
%                     \hline
%                     $CP^2$ & (Compressed ($\neg$U), Singleton)\hyperlink{fn1}{$^a$} \\
%                     \hline
%                     \emph{(UOP, CP) row-major} & (Dense, Compressed)\hyperlink{fn2}{$^b$} \emph{row-major} \\
%                     \hline
%                     \multicolumn{2}{l}{\emph{($\neg$U)} denotes a non-unique level.} \\
%                     \multicolumn{2}{l}{\hypertarget{fn1}{$^a$}Translated as \emph{(UOP, $CP^2$)} but the \emph{UOP} fiber is not necessary.} \\
%                     \multicolumn{2}{l}{\hypertarget{fn2}{$^b$}Translated as \emph{(U, UOP, CP)} but the \emph{U} fiber is not necessary.}
%                 \end{tabular}
%                 \label{tab:FTvsTACO_Ex}
%             \end{center}
%         \end{table}
%     \end{minipage}
% \end{figure}

%%%% VL %%%%
% \begin{figure}
%     \centering
%     \begin{minipage}[t]{0.42\linewidth}
%         \begin{table}[H] %htbp
%             \caption{Frequently Used SAFs}
%             \begin{center}
%                 \begin{tabular}{|c|c|}
%                     \hline
%                     U & Uncompressed \\
%                     \hline
%                     CP & Coordinate Payload \\
%                     \hline
%                     UOP & Uncompressed Offset Pairs \\
%                     \hline
%                     B & Bitmask \\
%                     \hline
%                     RLE & Run Length Encoding \\
%                     \hline
%                 \end{tabular}
%                 \label{tab:SAFs}
%             \end{center}
%         \end{table}
%     \end{minipage}
%     \hfill
%     \begin{minipage}[t]{0.56\linewidth}
%         \begin{table}[H] %htbp
%             \caption{SAFs Examples}
%             \begin{center}
%                 \begin{tabular}{|c||c|c|c|}
%                     \hhline{-||---}
%                     \textbf{Format} & \textbf{Number} & \textbf{Unified} & \textbf{Storage} \\
%                     \textbf{Name} & \textbf{of \emph{Ranks}} & \textbf{Format Name} & \textbf{Order} \\
%                     \hhline{=::===}
%                     V & 1 & \emph{U} & \\
%                     \hhline{-||---}
%                     SpV & 1 & \emph{CP} & \\
%                     \hhline{-||---}
%                     COO & 2 & \emph{CP$^{2}$} & \\
%                     \hhline{-||---}
%                     CSR & 2 & \emph{(UOP, CP)} & \emph{row-major} \\
%                     \hhline{-||---}
%                     CSC & 2 & \emph{(UOP, CP)} & \emph{column-major} \\
%                     \hhline{-||---}
%                     ELL & 2 & \emph{(U, CP)} & \emph{row-major} \\
%                     \hhline{-||---}
%                     HYB & 2, 2 & \emph{(U, CP), CP$^{2}$} & \emph{row-major} \\
%                     \hhline{-||---}
%                     DIA & 3 & \emph{(CP, UOP, CP)} & \emph{diagonal-major} \\
%                     \hhline{-||---}
%                     BCSR & 4 & \emph{(UOP, CP, U$^{2}$)} & \emph{row-major} \\
%                     \hhline{-||---}
%                 \end{tabular}
%                 \label{tab:SAFs_ex}
%             \end{center}
%         \end{table}
%     \end{minipage}
% \end{figure}
%%%%%%%%%%%%

%%%% VL %%%%
% \begin{figure}
%     \centering
%     \begin{subfigure}[t]{\textwidth}
%         \begin{subfigure}[t]{0.2414\textwidth} % 7/29
%             \centering
%             \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/Dense.pdf}
%             \caption{\textbf{Dense}: \emph{U$^{2}$}}
%             \label{fig:FormatEx:Dense}
%         \end{subfigure}
%         \hfill
%         \begin{subfigure}[t]{0.3448\textwidth} % 10/29
%             \centering
%             \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/COO.pdf}
%             \caption{\textbf{COO}: \emph{CP$^{2}$ (row-maj. sorted)}}
%             \label{fig:FormatEx:COO}
%         \end{subfigure}
%         \hfill
%         \begin{subfigure}[t]{0.3448\textwidth} % 10/29
%             \centering
%             \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/CSR.pdf}
%             \caption{\textbf{CSR}: \emph{(UOP, CP) row-maj.}}
%             \label{fig:FormatEx:CSR}
%         \end{subfigure}
%     \end{subfigure}
%     \par\bigskip
%     \begin{subfigure}[b]{\textwidth}
%         \begin{subfigure}[b]{0.3448\textwidth} % 10/29
%             \centering
%             \begin{subfigure}[t]{0.7000\textwidth} % 7/10
%                 \centering
%                 \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/ELL.pdf}
%                 \caption{\textbf{ELL}: \emph{(U, CP) row-maj.}}
%                 \label{fig:FormatEx:ELL}
%             \end{subfigure}
%             \par\bigskip
%             \begin{subfigure}[b]{0.8000\textwidth} % 8/10
%                 \centering
%                 \includegraphics[width=0.7500\textwidth]{Figures/PDF/SparseFormats/HYB.pdf} % 6/8
%                 \caption{\textbf{HYB}: \emph{(U, CP) row-maj., CP$^{2}$}}
%                 \label{fig:FormatEx:HYB}
%             \end{subfigure}
%         \end{subfigure}
%         \hfill
%         \begin{subfigure}[b]{0.5862\textwidth} % 17/29
%             \centering
%             \begin{subfigure}[t]{0.9412\textwidth} % 16/17
%                 \centering
%                 \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/BCSR.pdf}
%                 \caption{\textbf{BCSR}: \emph{(UOP, CP, U$^{2}$) row-maj.}}
%                 \label{fig:FormatEx:BCSR}
%             \end{subfigure}
%             \par\bigskip
%             \begin{subfigure}[t]{0.9412\textwidth} % 16/17
%                 \centering
%                 \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/DIA2.pdf}
%                 \caption{\textbf{DIA}: \emph{(CP, U) diagonal-maj.}}
%                 \label{fig:FormatEx:DIA2}
%             \end{subfigure}
%             \par\bigskip
%             \begin{subfigure}[t]{0.5882\textwidth} % 10/17
%                 \centering
%                 \includegraphics[width=\textwidth]{Figures/PDF/SparseFormats/DIA1.pdf}
%                 \caption{\textbf{DIA}: \emph{(CP, UOP, CP) diagonal-maj.}}
%                 \label{fig:FormatEx:DIA1}
%             \end{subfigure}
%         \end{subfigure}
%     \end{subfigure}
%     \caption{An Example of a Sparse Matrix Stored in Several Compressed Formats (every cell corresponds to a word in memory, including those which are empty).}
%     \label{fig:FormatEx}
% \end{figure}

% The nomenclature from~\cite{Wu2022_SparseLoop} is more precise than the common format names. For example, the \emph{DIA} format describes the principle of storing the values around the diagonal, that is an outer level of \emph{(CP, ...)} that is \emph{diagonal-major}. However, the values along the diagonals could themselves be stored in a dense or sparse format. If the diagonals are dense, this corresponds to \emph{(CP, U)}~\cite{Chou2018_COO_CSR_DIA_ELL_HASH}, as shown in Figure~\ref{fig:FormatEx:DIA2}. If the diagonals are stored with a \emph{CSR} like storage, then it would be referred to as \emph{(CP, UOP, CP)}, as shown in Figure~\ref{fig:FormatEx:DIA1}. Other variants of \emph{DIA} are also possible, for example, in \cite{Barrett1994_CSR_BCSR_DIA_ELL, Saad2003_COO_CSR_DIA_ELL, Bell2009_HYB} they propose to store the diagonals in a dense, \emph{column-major} two-dimensional table which corresponds to \emph{(CP, U, U)}.
%%%%%%%%%%%%

% %%%%%%%%%%%%%%%%%%%%%%%%%% Algorithms %%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Algorithms}
\label{sec:background:algos}

The accelerators described in this paper address sparse MV and MM. For MV we use the notation $A(M,K) \times b(K) = c(M)$ and for MM the notation $A(M,K) \times B(K,N) = C(M,N)$. The \emph{rank} \emph{K} is the \emph{common rank} between the two inputs.

The existence of different \emph{ranks} allows several algorithmic possibilities to compute matrix multiplication: the \emph{inner-product} algorithm (\algo{IP}), the \emph{outer-product} algorithm (\algo{OP}) and \emph{Gustavson}'s algorithm (\algo{Gust})~\cite{Faddeev1981_InnerOuter, Gustavson1978_GustAlgo}. In all cases, the matrix $A$ is traversed sequentially.
The three algorithms, however, impose different access patterns for $B$ (or $b$) and $C$ (or $c$). Indeed, in many cases, they have to be accessed multiple times, as discussed in the following sections.

\subsubsection{Inner-Product Algorithm}

The \emph{inner-product} algorithm~\cite{Faddeev1981_InnerOuter} is an intuitive way to calculate a matrix multiplication because it follows the loop order of the mathematical formulas~(\ref{eq:background:MVinner}) and~(\ref{eq:background:MMinner}) for MV and MM, respectively.

\begin{figure}[H]
    \begin{minipage}[m]{0.40\linewidth}
        \begin{equation}
            \forall m \in \left[0,M\right[, c_{m} = \sum_{k = 0}^{K-1} A_{m,k} \times b_{k}
            \label{eq:background:MVinner}
        \end{equation}
    \end{minipage}
    \hfill
    \begin{minipage}[m]{0.55\linewidth}
        \begin{equation}
            \forall (m,n) \in \left[0,M\right[ \times \left[0,N\right[, C_{m,n} = \sum_{k = 0}^{K-1} A_{m,k} \times B_{k,n}
            \label{eq:background:MMinner}
        \end{equation}
    \end{minipage}
\end{figure}

%%%% VC %%%%
The \emph{common rank} K is the inner-most loop of the algorithm (see Algorithm~\ref{alg:background:MMinner}).
%%%% VL %%%%
% The \emph{common rank} K is the inner-most loop of the algorithm (see Algorithms~\ref{alg:MVinner} and~\ref{alg:MMinner}).
%%%%%%%%%%%%
The output $C$ (or $c$) is updated sequentially, but the $B$-matrix (or $b$-vector) will be entirely read M times. In the context of sparse matrices, the reads of $B$ (or $b$) are conditioned by the non-zero elements of $A$, which requires indirect accesses. If $B$ (or $b$) is also sparse, the algorithm needs to perform \emph{intersections} between the non-zero elements in the rows of $A$ and the columns of $B$ (or in the $b$-vector) (see Section~\ref{sec:background:hw_challenges}). In Algorithm~\ref{alg:background:MMinner}, exchanging M and N simply causes the output to be updated \emph{column-wise}.

%%%% VL %%%%
% \begin{figure}
%     \begin{minipage}[t]{0.49\linewidth}
%         \begin{algorithm}[H]
%         \caption{MV Inner-Product Algorithm}\label{alg:MVinner}
%         \For{$m \in \left[ 0, M \right[$}{
%             \For{$k \in \left[ 0, K \right[$}{
%                 $c_{m} += A_{m,k} \times b_{k}$\;
%             }
%         }
%         \end{algorithm}
%     \end{minipage}
%     \hfill
%     \begin{minipage}[t]{0.49\linewidth}
%         \begin{algorithm}[H]
%         \caption{MM Inner-Product Algorithm}\label{alg:MMinner}
%         \For{$m \in \left[ 0, M \right[$}{
%             \For{$n \in \left[ 0, N \right[$}{
%                 \For{$k \in \left[ 0, K \right[$}{
%                     $C_{m,n} += A_{m,k} \times B_{k,n}$\;
%                 }
%             }
%         }
%         \end{algorithm}
%     \end{minipage}
% \end{figure}
%%%%%%%%%%%%

\subsubsection{Outer-Product Algorithm}

The \emph{outer-product} algorithm~\cite{Faddeev1981_InnerOuter} has the opposite \emph{rank} loop order than the \emph{inner-product}. It changes nothing mathematically except that the computation is separated into two phases. First, during the \emph{multiply} phase, we compute several \emph{partial outputs} (\emph{POs}) consisting of the \emph{partial sums} $A_{m,k} \times b_{k}$, then during the \emph{merge} phase, we accumulate all the \emph{POs} to form the final $C$-matrix (or $c$-vector).
%%%% VC %%%%
Algorithm~\ref{alg:background:MMouter} show that the \emph{common rank} K is the outer-most loop. For MM, exchanging M and N affects whether the output $C$ is updated by rows or by columns, but has no influence on the indirect accesses to the inputs.
%%%% VL %%%%
% Algorithms~\ref{alg:MVouter} and~\ref{alg:MMouter} show that the \emph{common rank} K is the outer-most loop. Inverting M and N affects whether the output $C$ (or $c$) is updated by rows or by columns, but has no influence on the indirect accesses to the inputs.
%%%%%%%%%%%%
%%%% VL %%%%
% \begin{figure}
%     \begin{minipage}[t]{0.49\linewidth}
%         \begin{algorithm}[H]
%         \caption{MV Outer-Product Algorithm}\label{alg:MVouter}
%         \For{$k \in \left[ 0, K \right[$}{
%             \For{$m \in \left[ 0, M \right[$}{
%                 $c_{m} += A_{m,k} \times b_{k}$\;
%             }
%         }
%         \end{algorithm}
%     \end{minipage}
%     \hfill
%     \begin{minipage}[t]{0.49\linewidth}
%         \begin{algorithm}[H]
%         \caption{MM Outer-Product Algorithm}\label{alg:MMouter}
%         \For{$k \in \left[ 0, K \right[$}{
%             \For{$m \in \left[ 0, M \right[$}{
%                 \For{$n \in \left[ 0, N \right[$}{
%                     $C_{m,n} += A_{m,k} \times B_{k,n}$\;
%                 }
%             }
%         }
%         \end{algorithm}
%     \end{minipage}
% \end{figure}
%%%%%%%%%%%%
With \emph{outer-product}, the $B$-matrix (or $b$-vector) is read sequentially. This algorithm necessitates the generation of K \emph{POs}. Moreover, in the context of sparse input matrices, the \emph{PO} element updates are conditioned by the non-zeros elements of both inputs. Thus, a \emph{partial sum} could potentially be zero, reducing the number of \emph{PO} element updates, and making them non-sequential. If the output $C$ (or $c$) is stored in a \emph{sparse format}, then it is necessary to repeatedly access and update each \emph{partial sum}.

%%%% VC %%%%
\begin{figure}
    \begin{minipage}[t]{0.33\linewidth}
        \begin{algorithm}[H]
        \caption{\newline MM \emph{Inner-Product} Algorithm}\label{alg:background:MMinner}
        \For{$m \in \left[ 0, M \right[$}{
            \For{$n \in \left[ 0, N \right[$}{
                \For{$k \in \left[ 0, K \right[$}{
                    $C_{m,n} += A_{m,k} \times B_{k,n}$\;
                }
            }
        }
        \end{algorithm}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.33\linewidth}
        \begin{algorithm}[H]
        \caption{\newline MM \emph{Outer-Product} Algorithm}\label{alg:background:MMouter}
        \For{$k \in \left[ 0, K \right[$}{
            \For{$m \in \left[ 0, M \right[$}{
                \For{$n \in \left[ 0, N \right[$}{
                    $C_{m,n} += A_{m,k} \times B_{k,n}$\;
                }
            }
        }
        \end{algorithm}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.33\linewidth}
        \begin{algorithm}[H]
        \caption{\newline MM \emph{Gustavson's} Algorithm}\label{alg:background:MMgustavson}
        \For{$m \in \left[ 0, M \right[$}{
            \For{$k \in \left[ 0, K \right[$}{
                \For{$n \in \left[ 0, N \right[$}{
                    $C_{m,n} += A_{m,k} \times B_{k,n}$\;
                }
            }
        }
        \end{algorithm}
    \end{minipage}
\end{figure}
%%%%%%%%%%%%

\subsubsection{Gustavson's Algorithm}

As the MM algorithm has three \emph{ranks} instead of two for MV, there is a third ordering, known as \emph{Gustavson's} algorithm~\cite{Gustavson1978_GustAlgo} where the \emph{common rank} K is the middle loop (see Algorithm~\ref{alg:background:MMgustavson}). With this approach, the output is computed row-by-row with \emph{PO}-rows generated and accumulated for each output $C$-row.

%%%% VL %%%%
% \begin{figure}
%     %\raggedright
%     \begin{minipage}[t]{0.49\linewidth}
%         \begin{algorithm}[H]
%         \caption{MM Gustavson's Algorithm}\label{alg:MMgustavson}
%         \For{$m \in \left[ 0, M \right[$}{
%             \For{$k \in \left[ 0, K \right[$}{
%                 \For{$n \in \left[ 0, N \right[$}{
%                     $C_{m,n} += A_{m,k} \times B_{k,n}$\;
%                 }
%             }
%         }
%         \end{algorithm}
%     \end{minipage}
% \end{figure}
%%%%%%%%%%%%

With this algorithm, updates of the $C$-rows are sequential, but, in the dense case, the $B$-matrix is entirely read M times and K \emph{PO}-rows of size N are generated. In the context of sparse matrices, the selection of the rows in $B$ is indirect, however, within a row the accesses are sequential. The \emph{intersection} operations are only needed for the $B$-rows and not each element. The \emph{PO}-rows can be accumulated before computing the next $C$-row. For this algorithm,  exchanging M and N causes the three matrices to be accessed \emph{column-wise} instead of \emph{row-wise}.

% \begin{table}
%     \begin{oldsec}
%     \centering
%     \caption{Comparison of Matrix Multiplication Algorithms}
%     \footnotesize
%     \begin{tabular}{|>{\centering\arraybackslash}m{1.25cm}|| *{3}{>{\arraybackslash}m{4.1cm}|}}
%         \hhline{-||---}
%         & \textbf{Inner-product (\algo{IP})} & \textbf{Outer-product (\algo{OP})} & \textbf{Gustavson (\algo{Gust})} \\
%         \hhline{=::===}
%         \textbf{A$(M, K)$} \newline \textbf{Accesses} & $\bullet$ read N times\hyperlink{fn3}{$^{\mathrm{1}}$} \newline $\bullet$ \emph{row-wise} \newline $\bullet$ sequential & $\bullet$ read once \newline $\bullet$ \emph{column-wise} \newline $\bullet$ sequential & $\bullet$ read once \newline $\bullet$ \emph{row-wise} \newline $\bullet$ sequential \\
%         \hhline{-||---}
%         \textbf{B$(K, N)$} \newline \textbf{Accesses} & $\bullet$ read $\overline{NNZ_{r}}(A) \times M$ times \newline $\bullet$ \emph{column-wise} \newline $\bullet$ not sequential & $\bullet$ read $\overline{NNZ_{c}}(A)$ times\hyperlink{fn4}{$^{\mathrm{2}}$} \newline $\bullet$ \emph{row-wise} \newline $\bullet$ sequential & $\bullet$ read $\overline{NNZ_{r}}(A)$ times\hyperlink{fn4}{$^{\mathrm{2}}$} \newline $\bullet$ \emph{row-wise} \newline $\bullet$ sequential within rows \\
%         \hhline{-||---}
%         \textbf{C$(M, N)$} \newline \textbf{Accesses} & $\bullet$ sequential \newline $\bullet$ generated \emph{row-wise} & $\bullet$ generated without specific structure \newline $\bullet$ K \emph{POs} produced (each \emph{PO} has $NNZ_{c}(A) \times NNZ_{r}(B)$ non-zero elements) & $\bullet$ generated \emph{row-wise} \newline $\bullet$ for each row, $NNZ_{r}(A)$ \emph{PO} rows produced (each \emph{PO} rows has $NNZ_{r}(B)$ non-zero elements) \\
%         \hhline{-||---}
%         \multicolumn{4}{l}{\hypertarget{fn3}{$^{\mathrm{1}}$}In the case of MV multiplication, $N=1$.} \\
%         \multicolumn{4}{l}{\hypertarget{fn4}{$^{\mathrm{2}}$}Average number of non-zero entries in the rows/columns of $A$.}
%     \end{tabular}
%     \label{tab:AlgosCompSum}
%     \end{oldsec}
% \end{table}

\subsection{Tiling}
\label{sec:background:tiling}

The \emph{tiling} process~\cite{Grunbaum1987_Tiling} consists in matrix partitioning: a matrix may be divided into \emph{tiles}, non-overlapping sub-matrices. In other words, this method consists of adding \emph{ranks} to delimit the \emph{tiles}, and thus adding loop iterations in the kernel algorithm.

\emph{Tiling} methods allow to locally reduce the dimensions of data and thus to adapt the working set to the size of the local memory~\cite{Lam1991_Tiling}, particularly when the matrix has a structure with denser regions. Tiling can also increase parallelism opportunities. Software optimizations for sparse matrix computation, such as OSKI~\cite{Vuduc2005}, are based on \emph{tiling}.
In previous sections, the study of the \algo{IP}, \algo{OP} and \algo{Gust} algorithms highlighted the influence of \emph{rank} ordering on the sequentiality and data movement, and thus, adding intermediate \emph{ranks} with \emph{tiling} breaks the regularity of pure \algo{IP} or \algo{OP} algorithms, requiring additional hardware support (see Section~\ref{sec:background:hw_challenges}).

For example, let us consider the $A$-matrix in Figure~\ref{fig:background:tiling_ex} which is divided into four tiles (one level of \emph{tiling}). For a SpMV, two new \emph{ranks} appear for a total of four \emph{ranks}: $M_1$ and $K_1$ allow \emph{tile} selection and $M_2$ and $K_2$ allow data access within a \emph{tile}. Thus, we could decide to apply an \algo{OP} on \emph{tiles} and conversely an \algo{IP} within \emph{tiles}, mixing-up the pros and cons of these two algorithms at different \emph{rank} levels. In the case of the example in Figure~\ref{fig:background:tiling_ex}, the upper-left $A$-\emph{tile} (red) is evaluated first, sequentially updating the upper tile of the $c$-vector in an \algo{IP} manner. The lower-left $A$-\emph{tile} (yellow) is then computed the same way. At this point, \emph{intersections} have been done with only the upper $b$-\emph{tile}, and $c$ has been updated sequentially. The two next $A$-\emph{tiles} (green, then blue) will compute \emph{intersections} with the lower $b$-\emph{tile} and update the entire $c$-vector sequentially, showing the \algo{OP} characteristic of having \emph{POs} to \emph{merge}, two in this case.

\begin{figure}
    \centering
    \includegraphics[width=0.4\linewidth]{Chapter_Background/Figures/PDF/Tiling_example.pdf}
    \caption{An Example of \emph{Tiling} on SpMV}
    \label{fig:background:tiling_ex}
\end{figure}

Note that in case of \emph{tiling}, the $A$-matrix is no longer read sequentially, which can be problematic if it is stored in a classical \emph{sparse format} like \emph{CSR} that is specifically made for \emph{row-wise} accesses. Custom \emph{sparse formats} adapted for \emph{tiling} can be used \cite{Im2004_TilingBCSR}, but this requires a preprocessing conversion.

% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Hardware Challenges
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Hardware Challenges for Sparse Matrices and Vectors}
\label{sec:background:hw_challenges}

In the previous section we presented a brief background on matrix multiplication without considering the underlying hardware. Although the various \emph{sparse formats} reduce the memory footprint, they have the disadvantage that indirect accesses are required, which creates data-dependencies and reduces the effectiveness of caches. As we have seen, with all three algorithms, at least some of the accesses are non-sequential. The lack of spatial locality for these accesses reduces the benefit of having caches. In other words, a full cache line is loaded, but only a few entries are read or modified. Moreover, the matrices of interest are extremely large and even the non-zero elements, and associated \emph{metadata}, can not generally fit in the cache. Depending on the algorithm, successive accesses to the same element may be too far apart to benefit from temporal locality in the cache.
% TODO: find the reference

% Perhaps we can add a reference to Yves's background paper where it was stated
% that with typical sparse matrix formats ~50% of the data brought into the
% cache is unused.

%% INNER PRODUCT
In an \algo{IP} algorithm, the $A$-\emph{metadata} needs to be read to indirectly access the $B$-matrix (or $b$-vector), which may itself be stored in a \emph{sparse format}. In this case, the algorithm needs to check if the non-zero $A$-indices are present in $B$ (or $b$). If an index appears in neither list, there is no need to perform the multiplication (\emph{ineffectual operation}). More generally, this problem applies to \emph{fibers} and is called the \emph{intersection} search. As a minimum, this requires reading all the \emph{metadata} for $A$ and $B$ (or $b$), and identifying those present in both.

%%%% VL %%%%
% In Algorithm~\ref{alg:InnerCSR}, we present the pseudo-code for an \emph{inner-product} SpMSpM with $A$ and $B$ stored in \emph{(UOP, CP) row-major} (\emph{CSR}) and \emph{column-major} (\emph{CSC}) formats, respectively. The outer loop~(\emph{M}), iterates over the rows of $A$, and the first step is to identify the start (\emph{A\_current\_ptr}) and end column (\emph{A\_next\_ptr}) in the \emph{CSR} \emph{metadata}. The middle-loop (\emph{n}) iterates over the columns of $B$. The inner-loop iterates over the elements of the current row of $A$, and for each index (\emph{A\_pos}), a search is performed in the \emph{metadata} of $B$, to see if the corresponding index in $B$ is present. Assuming it is ($B\_pos \neq -1$), the \emph{partial sum} is computed and then accumulated. This example highlights the need to efficiently search for the intersection of indices in the \emph{metadata} of the inputs.

% Different algorithms can be used to address the \emph{intersection} search, depending on whether the elements are sorted. When the elements are sorted, the \emph{intersection} becomes a merge and generally requires $2 \times k-1$ comparisons (in some cases the number of comparisons can be reduced~\cite{Baeza-Yates2010_Intersection}). As we will see in Section~\ref{sec:hw_accelerators}, certain accelerators provide support for performing this intersection operation directly in hardware.

% \begin{algorithm}
% \caption{Inner-Product SpMSpM Performing \emph{Intersection} Searches with \emph{(UOP, CP)} Format}\label{alg:InnerCSR}
% \KwData{$A$ in \emph{(UOP, CP) row-maj.} format (CSR): $A_{DATA}, A_{IND}, A_{PTR}$}
% \KwData{$B$ in \emph{(UOP, CP) column-maj.} format (CSC): $B_{DATA}, B_{IND}, B_{PTR}$}
% \KwData{$C$ in dense format (for simplicity of code)}
% \KwResult{$C = A \times B$}
% \For(\Comment*[f]{Iterations over the rows of A}){$m \in \left[ 0, M \right[$}{
%     $A\_current\_ptr \gets A_{PTR}(m)$\Comment*[r]{Get pointer of the current row of A}
%     $A\_next\_ptr \gets A_{PTR}(m+1)$\Comment*[r]{Get pointer of the next row of A}
%     \For(\Comment*[f]{Iterations over the columns of B}){$n \in \left[ 0, N \right[$}{
%         $acc \gets 0$\;
%         \For(\Comment*[f]{Iterations over the elements of the current row of A}){$A\_pos \in \left[ A\_current\_ptr, A\_next\_ptr \right[$}{
%             $ind \gets A_{IND}(A\_pos)$\Comment*[r]{Get index from table position}
%             $B\_pos \gets SEARCH(ind, B_{IND}, B_{PTR}, n)$\Comment*[r]{If $ind$ exists in the n-th column of B, $SEARCH()$ return the corresponding $B\_pos$ in $B_{DATA}$. Else, it return $-1$.}
%             \If(\Comment*[f]{If indices did match}){$B\_pos \neq -1$}{
%                 $partial\_sum \gets A_{DATA}(A\_pos) \times B_{DATA}(B\_pos)$\Comment*[r]{Partial sum computation}
%                 $acc \gets acc + partial\_sum$\Comment*[r]{Accumulation}
%             }
%         }
%         $C_{m,n} \gets acc$\Comment*[r]{The result is written in memory}
%     }
% }
% \end{algorithm}
%%%%%%%%%%%%

%% OUTER PRODUCT
In an \algo{OP} algorithm, the inputs are accessed sequentially, but the updates of the output require indirect accesses. The pattern of the accesses, while the output is generated, is non-sequential and determined by the structure of the input. Furthermore, if these \emph{random}\footnote{We use the term \emph{random} to mean accesses are irregular and difficult to predict.} updates provoke cache misses where only one word within a cache-line is modified, the bandwidth to external memory is poorly utilized. 
The \algo{OP} algorithm generates several \emph{partial outputs} (\emph{POs}) that must be accumulated to obtain the final output. We can identify two strategies: \emph{immediate update} and \emph{deferred update}. In the former, the final output is immediately updated as each \emph{partial sum} is generated. If the output is stored using a \emph{sparse format}, either the index being updated does not yet exist, and the new \emph{partial sum} must be inserted. Or, if it exists, a lookup must be done to locate it, the update performed and the result written back. The problem associated with combining the \emph{POs} is called \emph{reduction}.

%%%% VL %%%%
% In Algorithm~\ref{alg:OuterCSR}, we present the pseudo-code for an \emph{outer-product} SpMSpM using \emph{immediate update} with A and $B$ stored in \emph{(UOP, CP) column-major} (\emph{CSC}) and \emph{row-major} (\emph{CSR}) formats, respectively. The outer loop~(\emph{K}), iterates over the rows of A and the columns of $B$ (common rank), and the first step is to identify the start (\emph{A\_current\_ptr, B\_current\_ptr}) and end fibers (\emph{A\_next\_ptr},\emph{B\_next\_ptr}) in the \emph{metadata}. The middle-loop (\emph{A\_pos}) iterates over the elements of the current column of A. The inner-loop (\emph{B\_pos}) iterates over the elements of the current row of $B$. Because we only iterate over the non-zero values, there is no need for a search. The \emph{partial sum} is then computed. Finally, it must be accumulated to $C_{A\_ind,B\_ind}$. These accesses are non-sequential, making it necessary to lookup the value in $C$, update it, and write it back, which is performed by the \emph{REDUCTION()} function in the pseudo-code.

% \begin{algorithm}
% \caption{Outer-Product SpMSpM Performing \emph{Reductions} with \emph{(UOP, CP)} Format}\label{alg:OuterCSR}
% \KwData{$A$ in \emph{(UOP, CP) column-maj.} format (CSC): $A_{DATA}, A_{IND}, A_{PTR}$}
% \KwData{$B$ in \emph{(UOP, CP) row-maj.} format (CSR): $B_{DATA}, B_{IND}, B_{PTR}$}
% \KwData{$C$ in dense format (for simplicity of code)}
% \KwResult{$C = A \times B$}
% \For(\Comment*[f]{Iterations over the columns of A and the rows of $B$}){$k \in \left[ 0, K \right[$}{
%     $A\_current\_ptr \gets A_{PTR}(k)$\Comment*[r]{Get pointer of the current column of A}
%     $A\_next\_ptr \gets A_{PTR}(k+1)$\Comment*[r]{Get pointer of the next column of A}
%     $B\_current\_ptr \gets B_{PTR}(k)$\Comment*[r]{Get pointer of the current row of $B$}
%     $B\_next\_ptr \gets B_{PTR}(k+1)$\Comment*[r]{Get pointer of the next row of $B$}
%     \For(\Comment*[f]{Iterations over the elements of the current column of A}){$A\_pos \in \left[ A\_current\_ptr, A\_next\_ptr \right[$}{
%         $A\_ind \gets A_{IND}(A\_pos)$\Comment*[r]{Get index from table position}
%         \For(\Comment*[f]{Iterations over the elements of the current row of $B$}){$B\_pos \in \left[ B\_current\_ptr, B\_next\_ptr \right[$}{
%             $B\_ind \gets B_{IND}(B\_pos)$\Comment*[r]{Get index from table position}
%             $partial\_sum \gets A_{DATA}(A\_pos) \times B_{DATA}(B\_pos)$\Comment*[r]{Partial sum computation}
%             $REDUCTION(partial\_sum, C, A\_ind, B\_ind)$\Comment*[r]{Accumulate $partial\_sum$ with the $C_{A\_ind, B\_ind}$ element ($C_{A\_ind, B\_ind} \gets C_{A\_ind, B\_ind} + partial\_sum$) (read, update and write operation).}
%         }
%     }
% }
% \end{algorithm}
%%%%%%%%%%%%

% This paragraph covers deferred updates
To avoid the complexities of the indirect accesses with \emph{immediate updates}, the \emph{partial sums} can be stored and the accumulation can be deferred. One such technique is called \emph{output batching}~\cite{Kiriansky2016_Milk}. With this technique, tuples containing the index and the \emph{partial sum} are streamed sequentially to memory. Then, during a second pass, they are read back and the accumulations are performed, an approach which results in sequential memory accesses during the first pass, with the downside that the volume of data transferred to memory is increased.
%%%% VL %%%%
% The different strategies for dealing with these updates are shown in Figure~\ref{fig:OutputUpdatesStategies}.
%%%%%%%%%%%%
As we will see, hardware accelerators may combine the \emph{immediate} and \emph{deferred update} techniques to optimize memory bandwidth.
%%%% VL %%%%
% \begin{figure}
%     \begin{minipage}[b]{0.69\linewidth}
%         \begin{figure}[H]% Does it go with a taxonomy in the comparison section?
%             \centering
%             \includegraphics[width=\textwidth]{Figures/PDF/OutputUpdates.pdf}
%             \caption{Strategies for Dealing with Random Output Updates in Outer Product Algorithms}
%             \label{fig:OutputUpdatesStategies}
%         \end{figure}
%     \end{minipage}
%     \hfill
%     \begin{minipage}[b]{0.29\linewidth}
%         \begin{figure}[H]
%             \centering
%             \includegraphics[width=\textwidth]{Figures/PDF/GatherScatter.pdf}
%             \caption{\emph{Gather} and \emph{Scatter} Illustration}
%             \label{fig:gatherscatter}
%         \end{figure}
%     \end{minipage}
% \end{figure}
%%%%%%%%%%%%
In Table~\ref{tab:background:AlgosHWComp}, we summarize the previously discussed advantages and hardware challenges for each algorithm, including \algo{Gust} which presents a trade-off between \algo{IP} and \algo{OP}.

\begin{table}
    \centering
    \caption{Comparison of Advantages and Challenges with Matrix Multiplication Algorithms}
    \footnotesize
    \begin{tabular}{|>{\arraybackslash\centering}m{0.70cm} >{\arraybackslash\centering}m{0.40cm}|| *{3}{>{\arraybackslash}m{4.02cm}|}}
        \hhline{--||---}
        & & \multicolumn{1}{c|}{\textbf{Inner-product (\algo{IP})}} & \multicolumn{1}{c|}{\textbf{Outer-product (\algo{OP})}} & \multicolumn{1}{c|}{\textbf{Gustavson (\algo{Gust})}} \\
        \hhline{==::===}
        \multirow{3}{0.70cm}{\textbf{Access Count}} & \multicolumn{1}{|c||}{\textbf{MV}} & \textbf{\textit{A}:} once ; \textbf{\textit{b}:} $NNZ(A)$ ; \textbf{\textit{c}:} once & \textbf{\textit{A}:} once ; \textbf{\textit{b}:} once ; \textbf{\textit{c}:} $NNZ_{r}(A)$ & \multicolumn{1}{c|}{\emph{N/A}} \\
        \hhline{~-||---}
        & \multicolumn{1}{|c||}{\textbf{MM}} & \textbf{\textit{A}:} $N$ ; \textbf{\textit{B}:} $NNZ_{r}(A).M$ ; \textbf{\textit{C}:} once & \textbf{\textit{A}:} once ; \textbf{\textit{B}:} $NNZ_{c}(A)$ \newline \textbf{\textit{C}:} $\sim NNZ_{r}(A).NNZ_{c}(B)/K$ & \textbf{\textit{A}:} once ; \textbf{\textit{B}:} $NNZ_{c}(A)$ \newline \textbf{\textit{C}:} $\sim NNZ_{r}(A).NNZ_{c}(B)/K$ \\
        \hhline{--||---}
        \multicolumn{2}{|>{\arraybackslash\centering}m{1.50cm}||}{\textbf{Sequential Accesses}} & Accesses to $A$ and $C$ & Accesses to $A$ and $B$ & Accesses to $A$ and $C$ \newline Accesses to $B$-rows \\
        \hhline{--||---}
        \multicolumn{2}{|>{\arraybackslash\centering}m{1.50cm}||}{\textbf{Input Format}} & \multicolumn{2}{l|}{\begin{minipage}{7.84cm} Accesses to $A$ and $B$ benefit from alternate formats (\emph{CSR} and \emph{CSC}) \end{minipage}} &  Allows consistent format for $A$ and $B$   \\
        \hhline{--||---}
        \multicolumn{2}{|>{\arraybackslash\centering}m{1.50cm}||}{\textbf{Parellism Opportunity}} & Possible with \emph{tiling} & Generation of \emph{POs} over \emph{common rank} of $A$ and $B$ & Reading $A$-rows and updating $C$-rows \\
        \hhline{--||---}
        \multicolumn{2}{|>{\arraybackslash\centering}m{1.50cm}||}{\textbf{Reuse Opportunity}} & Potentially $B$, but limited by its size & An $A$-column or $B$-row while computing \emph{POs} & $A$ set of $B$-rows, based on non-zero elements of $A$-rows \\ %Based on non-zero elements of $A$-rows, a set of $B$-rows are reused
        \hhline{--||---}
        \multicolumn{2}{|>{\arraybackslash\centering}m{1.50cm}||}{\textbf{Challenges}} & \emph{Intersection} search between non-zero elements in $A$ and $B$ & Storage for \emph{POs} or in-place  \emph{merge} and \emph{reduce} & \emph{Merge} and \emph{reduce} of  \emph{PO rows} \\
        \hhline{--||---}
    \end{tabular}
    \label{tab:background:AlgosHWComp}
\end{table}

%%%% VC %%%%
The issues presented with \algo{IP} and \algo{OP} can be generalized to two concepts: \emph{gather} and \emph{scatter}.
%%%% VL %%%%
% The issues presented with \emph{inner-product} and \emph{outer-product} can be generalized to two concepts: \emph{gather} and \emph{scatter} (see Figure~\ref{fig:gatherscatter}).
%%%%%%%%%%%%
The \emph{gather} problem exists when the inputs are indirectly accessed, and need to be read from memory using \emph{metadata} to present the processor with the correct terms for computing the \emph{partial sums}. Typically, with \algo{IP}, the \emph{gather} problem is more difficult.
The \emph{scatter} problem exists when the order in which the output matrix/vector is generated is not sequential and is typically associated with the \algo{OP} algorithm.
In the case of \emph{tiling}, a hardware accelerator must handle both \emph{gather} and \emph{scatter}, creating a need for hardware support for both \emph{intersection} searches and \emph{reductions}.

\subsection{SpMSpM Algorithm Analysis}
\label{sec:background:AnalyticalModel}

To better understand how the algorithms impact the number and types of memory accesses, we developed, and presented in Figure~\ref{fig:background:AnalyticalModel_SpMSpM}, a model based on memory accesses counts. We identify five memory access patterns: (1) those where a matrix is read only once, thus there is no opportunity for reuse, (2) where the accesses are sequential, but a line is read multiple times, (3) where lines are read consecutively, but the accesses within a line are \emph{random}, (4) where the lines are accessed \emph{randomly}, but within a line the accesses are sequential, and (5) where the matrix elements are accessed multiple times, and with a \emph{random} access pattern.

For each of the three algorithms, we counted the number of read and write accesses, based on the density of the input matrices. We plotted the number of memory accesses in logarithmic scale for SpMSpM, based on a square matrix with $M = 10,000$. Indeed, the number of non-zero elements in $C$ depends on the structure of the input matrices and in this model, we have assumed the non-zero entries in the input matrices are uniformly, randomly distributed. This corresponds to a \emph{worst case} for sparse matrices (no spatial and temporal locality). Banded input matrices would, for example, have fewer non-zero elements in $C$.
In each graph, we have separated the accesses into $A$ (bottom), $B$ (middle) and $C$ (top). The colour code shows the access pattern, based on the five categories described above.

In the top-row of Figure~\ref{fig:background:AnalyticalModel_SpMSpM} (graphs~\ding{192}, \ding{193} and~\ding{194}), we start by considering a naïve accelerator which has no local storage, thus all data comes from main memory. From these figures, we clearly see that \algo{IP} requires more accesses, as the $A$-matrix must be read N times (graph~\ding{192}). Whereas for \algo{OP} and \algo{Gust}, the $A$-matrix is read only once (thus the grey colour). For \algo{OP} and \algo{Gust} the total number of accesses is the same, however, with \algo{OP} (graph~\ding{193}), the accesses to $C$ are \emph{random} (red). With \algo{Gust} (graph~\ding{194}), the accesses to $B$ are sequential within the rows (orange) and the $C$-rows are generated sequentially (yellow), thus \algo{Gust} avoids having any fully \emph{random} accesses (red).

\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{Chapter_Background/Figures/PDF/AnalyticalModel_SpMSpM.pdf}
    \caption{Model of Memory Access Counts Depending on Algorithm and Local Memory Size}
    \label{fig:background:AnalyticalModel_SpMSpM}
\end{figure}

Since main memory accesses are the principal bottleneck, accelerators integrate local storage (such as buffers or caches). Thus, we consider the case of an accelerator with sufficient local memory to store one full row, per matrix. We assume this local storage is perfect (no misses) and re-calculate the number of remaining main memory accesses, as shown in the middle-row of Figure~\ref{fig:background:AnalyticalModel_SpMSpM} (graphs~\ding{195}, \ding{196} and~\ding{197}). The matrices are assumed to be initially stored in main memory, thus they must be accessed at least once.
As can be seen, for \algo{IP} (graph~\ding{195}), the number of $A$-accesses is reduced, because rows are reused, thus the total number of $A$-accesses becomes equal to that in \algo{OP} and \algo{Gust}. For \algo{OP} (graph~\ding{196}), the single row buffer greatly reduces the number of $B$-accesses, however, it does nothing for $C$ (despite the appearance, due to the logarithmic scale). For \algo{Gust} (graph~\ding{197}), the single row buffer is sufficient to cache the \emph{reduction} operations in $C$, and thus, it now has fewer overall accesses than \algo{OP}. It is also interesting to note that \algo{OP} and \algo{Gust} scale better than \algo{IP} with lower densities, as seen by the shallower slope.

Finally, we have considered the extreme case where an accelerator has a buffer of sufficient size to store two matrices, which is shown in the bottom-row of Figure~\ref{fig:background:AnalyticalModel_SpMSpM} (graphs~\ding{198}, \ding{199} and~\ding{200}). Although this case is not practical for large matrices, through the proper use of \emph{tiling} to locally reduce the dimensions of a sub-matrix, this case can be achieved at the level of a \emph{tile}. In this case, the total number of external accesses is equal for the three algorithms, as each matrix is accessed only once from main memory. For \algo{IP} (graph~\ding{198}), it is interesting to note that such a large buffer is needed to locally address the \emph{intersection} search in $B$. Similarly for \algo{OP} (graph~\ding{199}), a large storage is required to address the \emph{reductions} in $C$. For \algo{Gust}, in fact, it is not necessary to buffer the entire $B$-matrix. Indeed, buffering a few rows (corresponding to $NNZ_{r}(A)$) is sufficient to achieve the number of accesses shown in graph~\ding{200}. This corresponds to an intermediate design point (multiple row buffers for $B$) not explicitly shown in the figure (between graphs~\ding{197} and~\ding{200}).

When viewed overall, Figure~\ref{fig:background:AnalyticalModel_SpMSpM} highlights the benefits of \algo{Gust}. We clearly see that \algo{Gust} does not require any fully \emph{random} accesses (red). With a single row buffer, it is possible to address the \emph{reductions} in $C$. And, with a limited number of row buffers, the number of external memory accesses for $B$ can be reduced. Based on this analysis, it is clear that \algo{Gust} is the best algorithm for hardware implementation of SpMSpM.

% \begin{table}
%     \centering
%     \caption{Caption}
%     \label{tab:my_label}
%     \begin{tabular}{|c|| *{3}{>{\arraybackslash}m{4.15cm}|}}
%         \hhline{-||---}
%          & \textbf{Inner-Product (\algo{IP})} & \textbf{Outer-Product (\algo{OP})} & \textbf{Gustavson (\algo{Gust})} \\
%          \hhline{=::===}
%         \textbf{A$(M,K)$} & 0D: $NNZ(A) \times N$ \newline 1D: $NNZ(A)$ & 0D: $NNZ(A)$ & 0D: $NNZ(A)$ \\
%         \hhline{-||---}
%         \textbf{B$(K,N)$} & 0D: $NNZ_{r}(A) \times NNZ(B) \times M$ \newline $= NNZ(A) \times NNZ(B)$ \newline 1D: $NNZ($B$) \times M$ \newline 2D: $NNZ($B$)$ & 0D: $NNZ_{c}(A) \times NNZ_{r}($B$) \times K$ \newline $= NNZ_{c}(A) \times NNZ($B$)$ \newline 1D: $NNZ_{r}($B$) \times K = NNZ($B$)$ & 0D: $NNZ_{r}(A) \times NNZ_{r}($B$) \times M$ \newline $= NNZ(A) \times NNZ_{r}($B$)$ \newline 2D: $NNZ($B$)$ \\
%         \hhline{-||---}
%         \textbf{C$(M,N)$} & 0D: $NNZ(C) \times 2$ \newline (max: $M \times N \times 2$) & 0D: $NNZ_{c}(A) \times NNZ_{r}($B$) \times K \times 2$ \newline $= NNZ_{c}(A) \times NNZ($B$) \times 2$ \newline 2D: $NNZ(C) \times 2$ \newline (max: $M \times N \times 2$) & 0D: $NNZ_{r}(A) \times NNZ_{r}($B$) \times M \times 2$ \newline $= NNZ(A) \times NNZ_{r}($B$) \times 2$ \newline 1D: $NNZ(C) \times 2$ \newline (max: $M \times N \times 2$) \\
%         \hhline{-||---}
%     \end{tabular}
% \end{table}

%%%%%%%%%%%%%%%%%%%%

\clearpage