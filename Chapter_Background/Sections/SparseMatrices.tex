% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %% Background %%
% Sparse Matrices
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Sparse Matrices}
\label{sec:bg:spMatrices}

\lettrine{S}{parse} matrices are two dimensional arrays filled with many zeros and stored in compressed formats in memory.
These sparse matrices are commonly used in diverse fields such as linear algebra, where sparse matrix multiplication is used to solve linear systems of equations for scientific purposes or simulation, as well as graph analytics where the vertices of a graph are represented as a sparse matrix~\cite{Buluc2011}, or finally transformers or other similar AI features where weights and activations can be represented and processed as sparse matrices~\cite{Gale2020, Farina2024}.
In these contexts, matrices are large, with dimensions up to billions of elements, and highly sparse, with non-zero densities down to $10^{-7}$~\cite{Kolodziej2019_SuiteSparse}.

We note matrices with capital letters $A$, $B$, $C$, conversely to vectors $a$, $b$, $c$.
Their dimension are denoted using $M$, $N$ and $K$.
For example, we can define a square matrix $A$ of dimension $M$, or a matrix $B$ of dimension $(K, N)$.
We note $nnz$ the number of non-zero elements of a matrix, and $d$ the associated density.
With the example of a matrix $A(M, N)$: $$d_A = \frac{nnz_A}{M \times N}$$

\begin{highlight}{Definition}{LimeGreen}
    A \textbf{sparse matrix} is a matrix filled with many zeros.
\end{highlight}

\begin{highlight}{Note}{SkyBlue}
    In general, we consider a matrix to be highly sparse when its number of non-zero elements has the same order of magnitude than its dimension, $nnz \sim M$.
    But there is no intrinsic definition of the frontier between a sparse and a dense matrix.
    In this manuscript, we consider a matrix as sparse when it needs to be compressed in memory (see Section~\ref{sec:bg:sparse_formats}).
\end{highlight}

% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Banded Matrices}
\label{sec:bg:bandedMatrices}

Matrices used in scientific computation originate from physical modelling where a system of partial differential equations is discretized, resulting in a large sparse matrix.
In these systems, the forces between elements decreases quickly with their distance, creating \gls{banded} matrices.
Moreover, numerical techniques exist to transform matrices into \gls{banded} matrices~\cite{Luo1992_MatrixStruct, Mafteiu2014_MatrixStruct, Wang2014_MatrixStruct}.

A \gls{banded} matrix is a matrix which has a dense region around its main diagonal and which is sparser away from the diagonal.
To give a few visual examples, Figure~\ref{fig:bg:example_matrix_structure} shows the structure plots for four typical matrices found in the SuiteSparse Matrix Collection~\cite{Kolodziej2019_SuiteSparse}, having a high-density \gls{band} along the diagonal.
In Figure~\ref{fig:bg:banded_mat_def}, we illustrate a \gls{banded}, square matrix of dimension $M$.
We define $w_B$ as the width of the \gls{banded} region, or \gls{bw}, as shown in \textit{red} in the figure.
Sometimes it is easier to refer to the half-\gls{bw}, $w_{hB}$, which respects the relation $w_B = 2 \times w_\mathit{hB} - 1$.
The average density in the \gls{band} and out of the \gls{band} is defined as $d_\mathit{IB}$ and $d_\mathit{OB}$, respectively.
As in Figure~\ref{fig:bg:raefsky3}, in a \gls{PBanded} matrix, there are no elements outside the \gls{band}, thus $d_\mathit{OB} = 0$.

\begin{figure}[htbp]
    \centering
    \begin{minipage}[b]{0.76\linewidth}
        \begin{figure}[H]
            \centering
            \begin{subfigure}[t]{0.24\linewidth}
                \centering
                \includegraphics[width=\linewidth]{Chapter_Background/Figures/PDF/roadNet-CA.pdf}
                \caption{\textit{roadNet-CA}}
                \label{fig:bg:roadNet-CA}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.24\linewidth}
                \centering
                \includegraphics[width=\linewidth]{Chapter_Background/Figures/PDF/scircuit.pdf}
                \caption{\textit{scircuit}}
                \label{fig:bg:scircuit}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.24\linewidth}
                \centering
                \includegraphics[width=\linewidth]{Chapter_Background/Figures/PDF/msc10848.pdf}
                \caption{\textit{msc10848}}
                \label{fig:bg:msc10848}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.24\linewidth}
                \centering
                \includegraphics[width=\linewidth]{Chapter_Background/Figures/PDF/raefsky3.pdf}
                \caption{\textit{raefsky3}}
                \label{fig:bg:raefsky3}
            \end{subfigure}
            \caption{Examples of \Gls{banded} Matrix Structure}
            \label{fig:bg:example_matrix_structure}
        \end{figure}
    \end{minipage}
    \hfill
    \begin{minipage}[b]{0.22\linewidth}
        \begin{figure}[H]
            \centering
            \includegraphics[width=0.88\linewidth]{Chapter_Background/Figures/PDF/BandedM_def.pdf}
            \caption{Banded Matrix Definition}
            \label{fig:bg:banded_mat_def}
        \end{figure} 
    \end{minipage}
\end{figure}

\begin{highlight}{Definition}{LimeGreen}
    A \textbf{\gls{banded} matrix} is a sparse matrix which has a higher density of non-zero elements around the diagonal.
    A \textbf{\gls{PBanded} matrix} concentrates all its non-zero elements within a delimitated region around the diagonal.
    This region is called the \gls{band}.
\end{highlight}

\begin{highlight}{Note}{SkyBlue}
    The SuiteSparse Matrix Collection~\cite{Kolodziej2019_SuiteSparse} is a large, open source data base of sparse matrices from multiple real-world applications, many of which were provided by companies such as Boeing.
    It gathers around 3,000 matrices of various sizes, densities and structure, which are extensively used for benchmarking (see Chapter~\ref{cha:soa}).
\end{highlight}