% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %% State-of-the-Art %%
% Accelerators Comparison
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Comparison of Existing Accelerators}
\label{sec:soa:comparison_and_analysis}

\lettrine{I}{n} this section, we analyze and compare the accelerators described in Section~\ref{sec:soa:hw_accelerators}.
To maintain clarity and avoid information overload, we focus on a representative subset of these accelerators for the tables and analysis in Section~\ref{sec:soa:analysis}, though our observations apply more broadly.
Table~\ref{tab:soa:classificationMaturity} lists this subset chronologically with the corresponding research institutions.

Several trends emerge from this overview.
The United States (US) and China lead in accelerator development, with China showing particularly strong activity since 2023.
Most accelerators originate from universities and institutes, with MIT (US) notably contributing to ExTensor, PHI, SpArch, and Gamma, while ICT (China) contributed to GAS, ACES, and Mentor.
Industry participation is also evident: NVIDIA was involved in five accelerators (\eg EIE, ExTensor, SpArch, Flexagon, and Vesper), while companies such as Axelera, Intel, Meta, Microsoft, SiFive, and Zettaflops contributed to individual projects.
The research is geographically distributed across Canada, China, France, Hong Kong, India, Iran, Italy, Japan, Singapore, South Korea, Spain, Switzerland, the United Kingdom (UK), and the United States (US).

\begin{table}[htbp]
    \caption{Chronological Summary of a Representative Sparse Accelerator Subset}
    \label{tab:soa:classificationMaturity}
    \centering
    \begin{tblr}{
            width = {\linewidth},
            colspec = {X[3,c] X[1,c] X[14,c]}, % rowspec
            rows = {m, font=\footnotesize},
            colsep = {1pt},
            row{1-Z} = {rowsep=2pt, black!2},
            row{1} = {c, font=\bfseries\footnotesize, black!5},
            hspan = {minimal},
            % hlines, vlines,
            % rulesep = {-1pt},
            hline{1,Z} = {2pt}, % Z for last
            hline{2} = {1pt},
            vline{2-3} = {1-Z}{0.5pt},
        }
        %
        Name & Year & Institute / University \\
        %
        OuterSPACE~\cite{Pal2018_OuterSPACE}        & 2018 & Univ. of Michigan (US) / ASU (US) \\
        ExTensor~\cite{Hegde2019_ExTensor}          & 2019 & Univ. of Illinois (US) / NVIDIA (US) / MIT (US) \\
        PHI~\cite{Mukkara2019_PHI}                  & 2019 & CSAIL, MIT (US) / CMU (US) \\
        SPiDRE~\cite{Barredo2019_SPiDRE}            & 2019 & UPC, BSC (Spain) / Arm Research \\
        ITS~\cite{Sadi2019_ITS}                     & 2019 & CMU (US) \\
        SPU~\cite{Dadu2019_SPU}                     & 2019 & Univ. of California (US) \\
        SpaceA~\cite{Xie2021_SpaceA}                & 2021 & UCSB (US) \\
        Gamma~\cite{Zhang2021_Gamma}                & 2021 & CSAIL, MIT (US) \\
        SortCache~\cite{Srikanth2021_SortCache}     & 2021 & Georgia Tech (US) / Zettaflops (US) / Sandia Labs (US) \\
        ASA~\cite{Zhang2022_ASA}                    & 2022 & Lehigh Univ. (US) / LBNL (US) \\
        Spada~\cite{Li2023_Spada}                   & 2023 & THU (China) / Northwestern Univ. (US) / DAMO Academy (China) / SQI (China) \\
        Flexagon~\cite{Munoz-Martinez2023_Flexagon} & 2023 & Univ. of Murcia (Spain) / Georgia Tech (US) / NVIDIA (US) \\
        SSSR~\cite{Scheffler2023_SSSR}              & 2023 & ETH Zurich (Switzerland) / SiFive (US) / Axelera (France) / Bologna Univ. (Italy) \\
        GAS~\cite{Zhang2024_GAS}                    & 2024 & ICT (China) / UCAS (China) \\
        ACES~\cite{Lu2024_ACES}                     & 2024 & Illinois Tech (US) / ICT (China) \\
        \textbf{SpDCache}~[\ref{publ:SpDC}]         & 2024 & CEA (France), TIMA (France), UGA (France) \\
        IOPS~\cite{Sun2025_IOPS}                    & 2025 & SUSTech (China) \\
        DMSA~\cite{Nagahara2025_DMSA}               & 2025 & Tokyo Tech (Japan) \\
        %
    \end{tblr}
\end{table}

% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Benchmarks, Evaluation and Performance}

Up to this point, we have discussed the architecture of various accelerators, without reporting the performance they achieve.
Most of the matrices used for benchmarking come from the SuiteSparse Matrix Collection~\cite{Kolodziej2019_SuiteSparse} that contains a large number of matrices from diverse, real applications.
Domain specific matrix libraries are also used~\cite{Leskovec2014_SNAP, Smith2017_FROSTT, Azad2018_HipMCL, Reddi2020_MLPerf}, as well as some synthetic matrix generators~\cite{Ang2010_Graph500, Beamer2017_GAP}.
In Table~\ref{tab:soa:evaluation}, we note the number of matrices used for evaluation across our subset of accelerators.
Most evaluations employ around ten or several tens of matrices, with few exceeding one hundred (none in the table).
Matrix densities are typically below $10^{-2}$ and can be as low as $10^{-7}$.
Some accelerators, such as ExTensor, Tensaurus, SortCache, and Flexagon, also include denser matrices in their benchmarks.
All studies evaluate large matrices with several million non-zeros, which is critical since these exceed typical cache sizes.
However, performance results often depend heavily on the matrices chosen.

Matrices are typically stored in straightforward formats like \acrshort{coo} (row-major sorted) within the SuiteSparse Matrix Collection, necessitating preprocessing for compatibility with certain accelerators.
For instance, some accelerators, such as EIE, ExTensor, Tensaurus, MatRaptor, Alrescha and C\textsuperscript{3}ache, require conversion to their specific custom formats.
Other \textit{standalone} accelerators utilize unique internal formats with \gls{tiling} methods but perform the conversion in hardware (\eg InnerSP, Spada, Flexagon, IOPS, DMSA).
Accelerators like Coup, PHI, SortCache, and ASA do not require preprocessing as they focus solely on \gls{scatter}.
\Gls{gather} focused accelerators such as SPiDRE and SSSR do not require preprocessing, as they directly stream matrices from standard sparse formats.
CoSPARSE is notably the only accelerator proposing to maintain two matrix copies in main memory (one in \acrshort{csr} and one in \acrshort{csc}) to enable joint software/hardware optimization.
Regardless, fair benchmarking must consider the costs associated with specialized preprocessing if any is required.

\begin{table}[htbp]
    \caption{Qualitative Analysis and Evaluation of Sparse Accelerators Subset}
    \label{tab:soa:evaluation}
    \centering
    \begin{tblr}{
            width = {\linewidth},
            colspec = {X[3,c] X[11,c] X[2,c] X[2.5,c]}, % rowspec
            rows = {m, font=\footnotesize},
            colsep = {3pt},
            row{1-Y} = {rowsep=2pt, black!2},
            row{1} = {c, font=\bfseries\footnotesize, black!5},
            row{Z} = {rowsep=2pt, font=\itshape\footnotesize},
            cell{Z}{1} = {c=4}{l},
            hspan = {minimal},
            % hlines, vlines,
            % rulesep = {-1pt},
            hline{1,Y} = {2pt}, % Z for last
            hline{2} = {1pt},
            hline{Z} = {1pt, dashed},
            vline{2-4} = {1-Z}{0.5pt},
        }
        %
        Name & Functional Evaluation (Tool / Evaluation Specifics / Host*) & Number of Matrices & Local Memory Size \\
        %
        OuterSPACE~\cite{Pal2018_OuterSPACE}        & \textit{gem5} / Model only key elements; Skip memory allocation; Ignore start-up time / $\varnothing$                & 20               & 528~KiB \\
        ExTensor~\cite{Hegde2019_ExTensor}          & Custom Python simulator / Only the \textit{Coordinator} is evaluated; Remainder modeled analytically / $\varnothing$ & 14               & $\sim$38~MiB \\
        PHI~\cite{Mukkara2019_PHI}                  & \textit{ZSim} / Entirely evaluated / 16-cores system with 3 level cache                                              & 6                & 32.3~MiB \\
        SPiDRE~\cite{Barredo2019_SPiDRE}            & \textit{gem5} / \textit{Unknown} / Single ARM core                                                                   & \textit{unknown} & $\varnothing$ (main memory size) \\
        ITS~\cite{Sadi2019_ITS}                     & $\varnothing$ / Evaluated on \acrshort{asic} and \acrshort{fpga} / $\varnothing$                                     & 31               & $\sim$64~MiB \\
        SPU~\cite{Dadu2019_SPU}                     & \textit{gem5} / SPU part implemented in Chisel; Control core simulated / $\varnothing$                               & 24               & $\sim$2.5~MiB \\
        SpaceA~\cite{Xie2021_SpaceA}                & Custom simulator / Simulation tracking values stored in 3D-\acrshort{dram} / $\varnothing$                                      & 15               & 4~GiB (main memory size) \\
        Gamma~\cite{Zhang2021_Gamma}                & Custom simulator / Entirely evaluated / $\varnothing$                                                                & 37               & 3~MiB \\
        SortCache~\cite{Srikanth2021_SortCache}     & Custom simulator / Entirely evaluated / Single threaded, in-order core  with 3 level cache                           & 11               & 16.3~MiB \\
        ASA~\cite{Zhang2022_ASA}                    & Modified \textit{ZSim} / Entirely evaluated; One ASA per core / 8-cores with 3 level cache                           & 9                & 8$\times$8~KiB \\
        Spada~\cite{Li2023_Spada}                   & Custom Rust simulator / Entirely simulated; RTL of key components / $\varnothing$                                    & 27               & 1.5~MiB \\
        Flexagon~\cite{Munoz-Martinez2023_Flexagon} & Custom simulator based on STONNE / Entirely evaluated / $\varnothing$                                                & 9                & 1.3~MiB \\
        SSSR~\cite{Scheffler2023_SSSR}              & $\varnothing$ / Entirely evaluated through RTL implementation / RISC-V Snitch System                                 & \textit{unknown} & 128~KiB \\
        GAS~\cite{Zhang2024_GAS}                    & HSPICE / Entirely simulated; Models for FeFETs and MOSFETs / 96 cores CPU, NVIDIA GPU                                & 20               & $\sim$40~MiB \\
        ACES~\cite{Lu2024_ACES}                     & Custom simulator / RTL of main components / $\varnothing$                                                            & 18               & 2~MiB \\
        \textbf{SpDCache}~[\ref{publ:SpDC}]         & Custom Python simulator / Entirely evaluated; Not cycle-accurate / Single-core system with one level cache           & 48               & 32~KiB \\
        IOPS~\cite{Sun2025_IOPS}                    & $\varnothing$ / Entirely evaluated through RTL implementation / $\varnothing$                                        & 10               & $\sim$77~KiB \\
        DMSA~\cite{Nagahara2025_DMSA}               & $\varnothing$ / Evaluated on \acrshort{fpga} / $\varnothing$                                                         & 10               & $\sim$22~MiB \\
        %
        * Mainly for \textit{processor-assist} accelerators.
    \end{tblr}
\end{table}

Not all accelerators have reached the same level of design maturity, and in Table~\ref{tab:soa:evaluation}, we have shown how each design of our subset of accelerators was analyzed or simulated.
Most designs report that they have been simulated at a cycle-accurate level.
Most of the accelerators use custom simulators or tools such as \textit{gem5}~\cite{Lowe2020_gem5}, \textit{ZSim}~\cite{SANCHEZ_2013_ZSIM} and STONNE~\cite{Munoz-Martinez2021_STONNE}, or HSPICE (Synopsys\textregistered) for in-memory computing, but unfortunately there is no common approach, making it difficult to fairly compare the reported performance.
RTL descriptions are often only generated for key parts of the design, except for few that directly use the RTL for evaluation (\eg EIE, ITS, SSSR, IOPS, DMSA).
To estimate power and area, authors used tools such as CACTI~\cite{Balasubramonian2017_CACTI7}, McPAT~\cite{Li2009_McPAT} and NVSim~\cite{Dong2012_NVSim}.
We have not attempted to compare power/energy as the data in the literature is heterogeneous and can not be fairly compared.
In the last column of the table, we show the size of the local memory storage used in simulation by our subset of accelerators.
Except for SPiDRE and SpaceA which work directly in main memory, the local memories are in the range of 32~KiB to 64~MiB, corresponding to 4k to 8.4M entries (if an entry is 8B).
Thus, in the model presented in Section~\ref{sec:bg:AnalyticalModel} of the previous chapter, most of the accelerators are situated in the middle-row of Figure~\ref{fig:bg:AnalyticalModel_SpMSpM}, with a local memory corresponding to a few matrix rows.
Few are closer from the bottom-row of Figure~\ref{fig:bg:AnalyticalModel_SpMSpM}, where matrices could be held entirely in local memory.

The accelerators performance are reported as a speedup versus a baseline, which may differ according to their hardware and software platforms: typically the Intel MKL library~\cite{Intel2003_IntelMKL}, the Armadillo library~\cite{Conrad2016_Armadillo}, SparseBLAS~\cite{Duff2002_SparseBLAS} for CPUs, or GridGraph~\cite{Zhu2015_GridGraph}, Gunrock~\cite{Wang2016_Gunrock} and GraphMat~\cite{Sundaram2015_GraphMat} specifically for graphs on CPUs, or cuSPARSE~\cite{NVIDIA2014_cuSPARSE} and CUSP~\cite{Dalton2014_Cusp} for GPUs.
Since different baselines with different numbers of threads and cores are used, these speedups need to be interpreted with care.
The speedups are in general higher for the \textit{standalone} accelerators as they provide optimized hardware for the entire kernel, while \textit{processor-assist} accelerators have less impressive speedups but aim to be general purpose and have lower area overhead.

The only accelerators that have a comparable baseline are some of the \textit{standalone} ones addressing sparse \acrshort{mm} kernels.
They all compare themselves to OuterSPACE with the same benchmark matrices, so we can sort them by their speedup: SPAGHETTI (13$\times$), Gamma (6.6$\times$), InnerSP (4.6$\times$), SpArch (4.0$\times$), SIGMA (3.0$\times$), IOPS (2.9$\times$), MatRaptor (1.8$\times$), Alrescha (1.7$\times$), MOSCON (1.1$\times$), OuterSPACE (1$\times$).
Some accelerators compare themselves against other accelerators beyond OuterSPACE, making direct speedup comparisons problematic.
An extended speedup ranking derived from indirect comparisons to OuterSPACE should be interpreted cautiously: GAS (26$\times$), SPAGHETTI (13$\times$), Sparm (7.9$\times$), Gamma (6.6$\times$), DMSA (6.2$\times$), InnerSP (4.6$\times$), SpArch (4.0$\times$), SIGMA (3.0$\times$), IOPS (2.9$\times$), GSCSp (1.8$\times$), MatRaptor (1.8$\times$), Alrescha (1.7$\times$), MOSCON (1.1$\times$), OuterSPACE (1$\times$).
Additionally, Spada, Flexagon, ACES, and Mentor can be indirectly compared to OuterSPACE through intermediate accelerators, though the resulting speedups lack precision due to propagated uncertainties: Spada (5.8$\times$-110$\times$), Flexagon (6.8$\times$-14$\times$), ACES (12$\times$-240$\times$), Mentor (8.6$\times$-25$\times$), and ScanNow (20$\times$-440$\times$).
This performance inconsistency stems from the absence of a common baseline and variations in benchmark matrices across studies.
Averaging the estimates for the three most recent accelerators yields: ScanNow (140$\times$), ACES (77$\times$), Spada (43$\times$), GAS (26$\times$), Mentor (14$\times$), SPAGHETTI (13$\times$), Flexagon (9.8$\times$), Sparm (7.9$\times$), Gamma (6.6$\times$), DMSA (6.2$\times$), InnerSP (4.6$\times$), SpArch (4.0$\times$), SIGMA (3.0$\times$), IOPS (2.9$\times$), GSCSp (1.8$\times$), MatRaptor (1.8$\times$), Alrescha (1.7$\times$), MOSCON (1.1$\times$), OuterSPACE (1$\times$).
These results warrant careful interpretation.
Even so, we observe that eleven of these nineteen \textit{standalone} accelerators employ the \acrshort{gust} algorithm (\eg ScanNow, ACES, Spada, Mentor, Flexagon, Sparm, Gamma, DMSA, InnerSP, GSCSp and MatRaptor), compared to seven using \acrshort{op} (\eg GAS, SPAGHETTI, Sparm, SpArch, IOPS, MOSCON and OuterSPACE) and four using \acrshort{ip} (\eg Sparm, SIGMA, IOPS and Alrescha).
This comparison suggests that \acrshort{gust} is the most suitable algorithm for high-performance hardware accelerators, with \acrshort{op} as the second choice.

These measurements are consistent with our model presented in Section~\ref{sec:bg:AnalyticalModel} (Figure~\ref{fig:bg:AnalyticalModel_SpMSpM}).
OuterSPACE, the baseline, corresponds to graph~\ding{196}.
MatRaptor, the first \acrshort{gust} accelerator, was able to achieve higher performance by reducing the number of \glspl{randAccess}, and creating better regularity (\textit{red} to \textit{yellow}), as seen in graph~\ding{197}.
SpArch and GAS are \acrshort{op} accelerators with a larger local memory, which is used to address the \textit{red} region in graph~\ding{196}.
Their $A$-matrix condensing method of MatRaptor, allows to reduce the number of \acrfullpl{po}, and approach the performance achievable in graph~\ding{199}, although, \textit{in fine}, it is impossible to store all the \acrshortpl{po} in local memory.
GAS leverages in-memory computing techniques to mitigate the limitations of the \acrshort{op} approach, thereby approaching the performance levels achievable in graph~\ding{199}.
\acrshort{gust} accelerators correspond to optimizations of the algorithm approach in graph~\ding{197}, to approach the performance in graph~\ding{200}.
This is mainly achieved by custom local memories and caches for the $B$-matrix to address the \textit{orange} region, given that it is only necessary to store a limited number of $B$-rows, and to get ahead-of-time information from the reading of the matrix $A$.
\acrshort{ip} accelerators face inherent challenges in achieving the performance levels of other approaches, a limitation also predicted by the model in Figure~\ref{fig:bg:AnalyticalModel_SpMSpM}.
Nevertheless, several recent accelerators (\eg CoSPARSE, Flexagon, Sparm, Vesper and IOPS) have demonstrated that \acrshort{ip} delivers competitive performance on certain matrices, comparable to \acrshort{op} and \acrshort{gust} approaches.
To capitalize on this observation, these designs support multiple or all three algorithms, enabling adaptive algorithm selection.
While this flexibility for \textit{standalone} accelerators increases hardware complexity, it yields substantial performance gains and drives the field toward adaptive dataflow architectures.
These findings validate that the model presented in Section~\ref{sec:bg:AnalyticalModel} provides valuable insights into local memory requirements for sparse \acrshort{mm} accelerators.

\begin{highlight}{Conclusion}{Red}
    This comparative analysis of state-of-the-art sparse matrix accelerators reveals that \textbf{\acrlong{gust}'s algorithm is the most effective for high-performance} when coupled with \textit{standalone} hardware implementations, \textbf{followed by \acrlong{op}} which is particularly leveraged in \textit{processor-assist} designs, with \acrlong{ip} showing competitive results in specific cases, highlighting the importance of algorithmic adaptability and tailored memory hierarchies in optimizing sparse computations.
\end{highlight}

% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Analysis for Designers}
\label{sec:soa:analysis}

There is no single ``best overall'' accelerator.
A designer must consider the required performance, the available area and the class of problems to be addressed.
In Figure~\ref{fig:soa:taxonomy} we present a flow diagram which can guide a designer towards an architecture based on their requirements.
Starting from the top-right of the figure, the designer must first decide between a \textit{specialized accelerator} for a specific kernel or a \textit{general purpose accelerator}.
The former is preferable if performance must be maximized, requiring a \textit{standalone} accelerator addressing both \gls{gather} and \gls{scatter} (\eg ITS, SpaceA, Gamma, Spada, Flexagon, ACES, IOPS and DMSA, from the subset of accelerators).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{Chapter_SoA/Figures/PDF/SoA_AnalysisForDesigners.pdf}
    \caption{Flow Diagram for Architecture Selection}
    \label{fig:soa:taxonomy}
\end{figure}

A \textit{general purpose accelerator} can either take the form of a highly configurable \textit{standalone} block (\eg OuterSPACE, ExTensor, SPU and GAS) or a \textit{processor-assist} that boosts performance for a specific task (\eg ExTensor bricks, PHI, SPiDRE, SortCache, ASA, SSSR and SpDCache).
With a \textit{processor-assist} (rightmost branch in the figure), the processor still performs important parts of the kernel, typically the multiplications, but off-loads the \gls{gather} or \gls{scatter} tasks to the accelerator, as these are the tasks which are the bottleneck with a traditional cache hierarchy.
For example, for an \acrshort{ip} algorithm, the difficult part of the \gls{gather} task is performing the \glspl{intersection} (\eg the ExTensor bricks, SPiDRE and SSSR).
Conversely, with \acrshort{op} or \acrshort{gust}, the challenge with the \gls{scatter} is to efficiently perform \glspl{reduction} (\eg PHI, SortCache, ASA and SpDCache).
Finally, the designer must decide whether the storage is done by reusing existing memory resources (\eg PHI, SPiDRE, SortCache and SpDCache) or through custom caches and memories.

Going back to the case of a \textit{standalone} accelerator, the algorithms differ according to the supported kernels.
For sparse \acrshort{mv}, most of the accelerators studied here have opted for an \acrshort{op} algorithm.
For sparse \acrshort{mm}, the designers of around nine of the eleven highest performing accelerators have chosen \acrshort{gust} algorithm, as this algorithm provides a good trade-off, simplifying the \gls{gather} by reusing input $B$-rows and the row-by-row generation of the output, simplifying the \gls{scatter} aspect.
The fact that the outputs are generated row-by-row, facilitates parallel implementations.
\acrshort{gust} works well with \acrshort{csr} or \acrshort{csc}, providing consistent input and output formats.
Beyond the basic algorithm, \gls{tiling} approaches or new dataflows mixing algorithms can be explored (\eg ExTensor, SPU, Spada, Flexagon, IOPS and DMSA).

Orthogonal to previous choices, other design considerations are important.
First, the starting point needs to be defined: some accelerators require software to perform \gls{tiling} (\eg ExTensor), to align data with \acrshort{dram}-blocks (\eg SpaceA), or to rearrange the rows to improve performance (\eg Gamma).
The cost of such preprocessing can be high, and is usually only justified if the same matrix is reused multiple times.

\begin{highlight}{Conclusion}{Red}
    This analysis provides a structured decision-making framework for designers, emphasizing that the choice between specialized (\textit{standalone}) and general-purpose (\textit{processor-assist}) accelerators \textbf{depends on performance needs, area constraints, and problem-specific requirements}, with algorithm selection (\eg \acrlong{gust} for sparse \acrshort{mm}, \acrlong{op} for sparse \acrshort{mv}) and memory optimization strategies playing pivotal roles in tailoring solutions to diverse sparse computing challenges.
\end{highlight}